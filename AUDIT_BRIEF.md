# وثيقة المطالبة — مهمة الجرد والتدقيق الشامل الحي (Full Live Audit Mandate)
## المشروع: فندق قلب القاهرة — عدن | Cairo Heart Hotel — Aden

> **طبيعة هذه الوثيقة:** أمر عمل مؤسسي ملزم (Work Order) موجّه إلى وكيل ذكاء اصطناعي تنفيذي يعمل بكامل قدراته وأدواته ومهاراته.
> **مخرجها الوحيد:** تقرير تدقيق كامل شامل غير منقوص مدعوم بالأدلة، بصيغة القسم 12 حرفيًا.
> **نسختها:** 1.0 — صادرة بأمر المالك، ومودَّعة في جذر المستودع باسم `AUDIT_BRIEF.md`.

---

## 0) روح المهمة والقواعد الثلاث المطلقة

أنت مكلف بعمل **جرد كامل متكامل من الأساس إلى الرأس**: سحب المشروع من المستودع، التعرف عليه وفهمه، وضع كل شيء في مكانه الصحيح، تثبيت وتنزيل وتجهيز كل ما يتطلبه الأمر، ثم **تنفيذ عمل فعلي حي — لا فحص نظري**: تشغيل حي واختبار حي للويب ولتطبيق الجوال، كلٌّ بحسب ما هو مطلوب منه، لاكتشاف **أي خلل من أي نوع** في: الشاشات، الصفحات، الوظائف، العمليات، المتطلبات، الاعتماديات، الألوان، الثيم، اللغة، المنطق، المحاسبة، والأقسام كافة — ثم إخراج التقرير الكامل.

**القواعد الثلاث المطلقة (لا استثناء واحد):**

1. **قاعدة الواقع:** كل حقيقة تستخلصها من المستودع نفسه أو من تشغيل حي فعلي. هذه الوثيقة تعطيك نقطة انطلاق مثبتة سلفًا لكنها لا تعفيك من التحقق. ممنوع الاعتماد على الذاكرة أو التعميم أو «المنطق السليم» بديلًا عن التنفيذ. **إن تعارضت هذه الوثيقة مع الواقع المستخرج من المستودع: الواقع يغلب، والتعارض يُسجَّل أول بند في التقرير.**
2. **قاعدة الدليل:** كل ادعاء — سلبيًا كان أم إيجابيًا («هذا يعمل» / «هذا معطوب») — يُرفق بدليل: مخرج أمر حرفي، لقطة شاشة، سطر سجل، أو موضع `ملف:سطر`. ادعاء بلا دليل = ادعاء ملغى لا مكان له في التقرير.
3. **قاعدة الإنجاز:** صيغة «تم فحص X» تعني حصريًا: شُغِّل، فُتِح، فُعِّل، جُرِّب، ورُصدت النتيجة. قراءة الكود وحدها ليست فحصًا — هي تمهيد للفحص.

**محظورات لغوية في التقرير نهائيًا:** «يبدو»، «أظن»، «على الأرجح»، «من المفترض أن يعمل». إما دليل أو صمت.

---

## 1) بطاقة المشروع — حقائق مثبتة سلفًا (نقطة انطلاقك، لا بديلًا عن التحقق)

| البند | القيمة المثبتة |
|---|---|
| المستودع | `https://github.com/Mohammed503-qtb/Cairo-Hart-Hotel-0.git` — فرع `main` |
| نقطة التحقق الموثقة عند إصدار هذه الوثيقة | commit `fb9edf2` (إغلاق F5 — Task 21) |
| الوصف العام | منصة فندقية أحادية قاعدة البيانات: موقع حجز عام + تطبيق ضيف + وضع استقبال + وضع إدارة + خدمة وقت فوري (Socket.IO) + تدقيق شامل |
| الويب | Next.js 16 (App Router) · TypeScript 5 · Tailwind CSS 4 · shadcn/ui (New York) + Lucide · Zustand · Framer Motion · socket.io-client · runtime: **Bun** |
| تقديم الويب | SPA بمسار واحد `/` فقط (`src/app/page.tsx`) يبدّل الأوضاع: website / login / guest / reception / admin — عربي RTL بخط Cairo + ليلي/نهاري |
| قاعدة البيانات | Prisma + SQLite · **22 نموذجًا** (`prisma/schema.prisma`) · المسار الرسمي الوحيد: migrations (`prisma/migrations/`) · ملف القاعدة `db/custom.db` **غير مؤرشف في المستودع — يُولَّد محليًا** |
| سطح الـ API | **67 ملف مسار / 82 نقطة نهاية** (`src/app/api/`) · العقود الموثقة الحرفية: `docs/CONTRACTS.md` (v1.1 — مجموعة F1 معلنة STABLE) |
| الوقت الفوري | خدمة مستقلة `mini-services/realtime`: عملاء Socket.IO على المنفذ **3002** عبر البوابة (`?XTransformPort=3002`) · بث داخلي من الخادم: `POST http://localhost:3004/emit` (محلي فقط) |
| الاختبارات (القيم المرجعية عند الإصدار) | ويب: **165** اختبارًا بأمر `bun run test` (قاعدة اختبار معزولة) · جوال: **169** اختبارًا `flutter test` — كلها خضراء في CI عند `fb9edf2` |
| CI/CD (GitHub Actions) | `web-ci.yml` · `mobile-ci.yml` (analyze+test+build smoke) · `mobile-release.yml` (APK موقّع → GitHub Releases؛ مفتاح التوقيع في Secrets لا المستودع) |
| تطبيق الجوال | Flutter (اختُبر مع 3.29.x) في `mobile/` · flavors: dev/prod · RTL + خط Cairo + كحلي/ذهبي · `--dart-define` لـ API_BASE_URL وAPP_VERSION |
| الدومين الجوهري | **Reservation** = وعد الحجز (من الموقع) ≠ **Stay** = الإقامة الفعلية (تُنشأ عند Check-In) · البيع على **Room Type** والغرفة تُخصص عند الوصول · **Price Snapshot** مجمّد وقت الحجز · **المال بالسنت الصحيح (أعداد صحيحة)** |
| الهويات | مرجع `HTL-2026-000NNN` (تتبع فقط — لا دخول أبدًا) · أكواد دخول H/R/A (بادئة + 7 رموز) مخزنة SHA-256 |
| الهوية البصرية | كحلي **`#1A3C6E`** + ذهبي **`#D4A843`** · خط Cairo · عربي RTL أولًا · ليلي/نهاري (ويب وجوال معًا) |
| حالة المراحل الموثقة | H ✅ · F0/F1/F2 ✅ · F4 ✅ · F5 ✅ · F6 ✅ · F3 (Push/FCM) معلّقة · F7 (Dogfooding) معلّقة · F8 (المتاجر) معلّقة · W0 مقفل بقرار المالك (wa.me فقط — بلا Twilio) · W1–W4 معلّقة · P/O لاحقًا |

### 1.1 أكواد الدخول التجريبية (بيانات seed — بيئة التطوير فقط)

| الدور | الكود | الحالة المزروعة |
|---|---|---|
| ضيف — خالد يوسف (غرفة 201) | `H834729X7` | إقامة جارية |
| ضيف — نورا يوسف (غرفة 103) | `H119922K4` | مغادرة «اليوم» وقت الزرع |
| استقبال | `R492671M3` | فعال |
| إدارة | `A371849L9` | فعال |

> **تنبيه انحراف التواريخ:** بيانات seed مزروعة بتواريخ ثابتة (2026). سجّل تاريخ تشغيلك الفعلي، وقارن الحالات المعروضة («مغادرة اليوم»، متأخرو الخروج…) بتقويمك الفعلي — أي انزياح زمني يُوثَّق كملاحظة في بُعد المنطق، ولا يُعد خللًا برمجيًا بذاته.

### 1.2 خريطة الوثائق (اقرأها كلها — ترتيب القراءة في القسم 4)

`README.md` (الجذر) · `MASTER_PLAN.md` (v2.5 — الخطة الحاكمة) · `worklog.md` (سجل التنفيذ الكامل حتى Task 21) · `docs/CONTRACTS.md` (1919 سطرًا — عقد الـ 82 نقطة) · `mobile/README.md` · `upload/PLAN_WEBSITE.md` (المتطلبات الأصلية للموقع) · `upload/PLAN_ MOBILE-APK.md` (المتطلبات الأصلية للتطبيق) · `docs/archive/MASTER_PLAN_v1.md` (الأرشيف) · `.github/workflows/*` (خطوط CI الثلاثة).

---

## 2) قيود بيئة التشغيل (ملزمة — مخالفتها تفسد الفحص)

1. **خادم التطوير فقط على المنفذ 3000** (`bun run dev`). **محظور نهائيًا:** `bun run build` أو أي تشغيل إنتاجي محلي — الفحص الحي كله عبر خادم التطوير. راقب سجل الخادم (`dev.log` في بيئة المشروع) بعد كل مرحلة واستخرج منه أخطاء التشغيل.
2. **الوصول عبر البوابة:** أي طلب HTTP/WebSocket موجّه لغير منفذ 3000 يُلحق به حقل الاستعلام `XTransformPort` (مثال عملي: اتصال Socket.IO يكون `io("/?XTransformPort=3002")` — المسار `/` دائمًا). ممنوع كتابة منافذ مباشرة في عناوين الطلبات عبر البوابة. الوصول الداخلي المباشر عبر `localhost` مسموح لوكيل الفحص نفسه (اختبارات API والبث الداخلي 3004).
3. **الملفات غير المؤرشفة:** `.env` و`db/*.db` غير موجودين في المستودع عمدًا — أنت تولّدهما في مرحلة التجهيز. لا يوجد أي سر آخر مطلوب: المتغير الوحيد هو `DATABASE_URL`.
4. **قاعدة المخطط الصارمة (H1/AD-09):** `db:push` محظور منعًا باتًا — كل شيء عبر `prisma migrate deploy`. أي تعديل مخطط (وهو محظور أصلًا في مهمتك) كان سيمر عبر ملف migration.
5. **لا أسرار في المستودع** — إن واجهت مصادقة لسحب المستودع استعمل بيانات الوصول التي يوفرها لك مشغّلك.

---

## 3) المرحلة صفر — السحب والتجهيز والتثبيت (Setup) + بوابة قبولها

نفّذ بالترتيب حرفيًا، وسجّل مخرج كل أمر كدليل:

```bash
# 1) السحب
git clone https://github.com/Mohammed503-qtb/Cairo-Hart-Hotel-0.git && cd Cairo-Hart-Hotel-0
git log --oneline -5          # وثّق نقطة الوقوف الفعلية

# 2) الاعتماديات (ويب)
bun install

# 3) ملف البيئة (المتغير الوحيد المطلوب)
cp .env.example .env
# عدّل DATABASE_URL إلى مسار مطلق لقاعدة بياناتك، مثال:
#   DATABASE_URL="file:/ABS/PATH/TO/Cairo-Hart-Hotel-0/db/custom.db"

# 4) بناء قاعدة البيانات — المسار الرسمي الوحيد (ممنوع db:push)
bunx prisma generate
bunx prisma migrate deploy    # ينشئ ملف القاعدة ويطبّق كل migrations بالترتيب
bunx prisma migrate status    # يجب: صفر انحراف — وثّق المخرج

# 5) بيانات العرض (فندق + 14 غرفة + خدمات + حجوزات + طاقم + الأكواد التجريبية)
bun prisma/seed.ts

# 6) خدمة الوقت الفوري (خدمة مستقلة — شغّلها في الخلفية في جلسة/طرفية مستقلة)
cd mini-services/realtime && bun install && bun run dev
#   → يجب أن ترى: "Realtime socket service running on port 3002" (وثّق المخرج)

# 7) تطبيق الويب (الجذر — خلفية مستقلة)
cd .. && bun run dev          # المنفذ 3000 حصرًا
```

**تحقق جهوزية البيئة (بوابة القبول — قبل أي فحص):**
- `GET /` يستجيب 200 والصفحة تُصيَّر بصريًا (وليست شاشة بيضاء أو صفحة خطأ).
- `GET /api/public/hotel` يعيد بيانات الفندق (JSON سليم).
- سجل الخادم خالٍ من أخطاء قاتلة (FATAL/Unhandled).
- خدمة realtime مرفوعة (3002 + 3004).
- **الجوال (إن توفر Flutter SDK):** `cd mobile && flutter pub get` — ثم جرّب `flutter --version` ووثّق توفر الأجهزة/المحاكيات.

**قاعدة الفشل في التجهيز:** إن فشل أي بند، فرّق بدقة بين (أ) خلل في بيئتك/أدواتك — تصلحه بنفسك بلا مساس بكود المشروع، و(ب) خلل حقيقي في المشروع — يُسجَّل عيبًا حرجًا أول التقرير، ثم تابع بقدر المستطاع. لا تتوقف كليًا عند أول عائق؛ وثّق واستمر.

---

## 4) المرحلة الأولى — التعرف والفهم (Recognition) + بوابة معايرة

اقرأ بالترتيب: `README.md` → `MASTER_PLAN.md` كاملًا (مع تركيز خاص على §2 الوضع الحالي، §3 قرارات ADR، §12 الثوابت وقواعد المال وآلات الحالة والتسمية، §13 الأكواد التجريبية) → `worklog.md` كاملًا (سجل كل مهمة ودروسها) → `docs/CONTRACTS.md` (اشكال الطلبات/الاستجابات ورسائل الأخطاء العربية الحرفية) → `mobile/README.md` → وثيقتا المتطلبات الأصليتان في `upload/`.

**بوابة المعايرة — أجب من المستودع نفسه قبل المتابعة (أجوبتك تدخل التقرير كقسم «الفهم المستخلص»):**
1. لماذا Reservation ≠ Stay؟ وما نقطة التحويل بينهما؟
2. ما الهويات الأربع في النظام، وأيها وحده وسيلة دخول؟ ولماذا يُعرض الكود الخام مرة واحدة فقط؟
3. أين يُفرض «المال بالسنت»؟ وما قاعدة الضريبة والرصيد الحرفية؟
4. كم نقطة نهاية في الـ API؟ وأين وثيقة عقدها؟ وما مجموعة F1 المعلنة STABLE؟
5. اذكر الثوابت I1–I12 بملخص سطر لكل ثابت.
6. اذكر آلات الحالة: Reservation · Stay · ServiceRequest · Room · AccessCode.
7. ما دور كل من H/R/A؟ وما البادئة وطول كل كود؟
8. ارسم مسار حدث لحظي كاملًا: ضيف يرسل طلب خدمة → ظهوره عند الاستقبال (بالمنافذ والغرف والأحداث).
9. ما المسار الوحيد المرئي في الويب؟ وكيف يتبدل الوضع بين الأدوار؟
10. ما المراحل المقفلة فعليًا وما المعلّق؟ وما الذي يحظر الخطة فتحه؟ (AD-06)
11. ماذا يحدث لفاتورة حجز قديم إذا رُفع سعر النوع لاحقًا؟ (I9)
12. ما الخدمات المستقلة عن تطبيق Next.js؟ ومنافذ كل منها؟

---

## 5) المرحلة الثانية — الفحص الساكن (بوابة تمهيدية — ليست هدف المهمة)

نفّذ وسجّل المخرجات الحرفية:

| # | الفحص | الأمر | المعيار المرجعي |
|---|---|---|---|
| S1 | جودة الكود | `bun run lint` | exit 0 |
| S2 | اختبارات الويب | `bun run test` | 165/165 خضراء (إن اختلف العدد: وثّق العدد الفعلي وتفسيره) |
| S3 | انضباط المخطط | `bunx prisma migrate status` | صفر انحراف |
| S4 | تحليل الجوال | `cd mobile && flutter analyze` (إن توفر SDK) | صفر إشكالات |
| S5 | اختبارات الجوال | `flutter test` (إن توفر SDK) | 169/169 |
| S6 | مطابقة العقد | عُدّ ملفات المسارات ونقاط النهاية فعليًا من `src/app/api/` وطابقها ضد `docs/CONTRACTS.md` | 67 ملفًا / 82 نقطة — أي انحراف يُوثَّق |
| S7 | تتبع المتطلبات | قارن `upload/PLAN_WEBSITE.md` و`upload/PLAN_ MOBILE-APK.md` و§2.1 من الخطة مقابل المنفَّذ فعليًا | كل متطلب: منفَّذ / غائب / منحرف — بجدول |
| S8 | الاعتماديات | راجع `package.json` و`mobile/pubspec.yaml`: تناقضات إصدارات، حزم بلا استخدام ظاهر، مخاطر أمنية معروفة (بحث ويب عند الحاجة) | توثيق كامل |

> **تحذير جوهري:** نجاح هذه البوابة كلها لا يعني أن المشروع سليم — التاريخ أثبت أن أخطاء حية توجد مع اختبارات خضراء. هذه البوابة شرط دخول للفحص الحي، لا بديلًا عنه. **المهمة الحقيقية تبدأ من القسم 6.**

---

## 6) المرحلة الثالثة — الفحص الحي للويب (قلب المهمة)

استعمل أداة متصفح آلي (agent-browser أو مكافئها في بيئتك). التقط لقطة شاشة لكل حالة وافحصها بصريًا (بقدرات فهم الصور إن توفرت). سجّل أخطاء console في كل صفحة. اختبر في ثلاثة أعمدة عرض: **375 / 768 / 1440**.

### 6.1 الموقع العام (زائر بلا تسجيل)
- الصفحة الرئيسية تُصيَّر: الواجهة الرئيسية (hero)، الأنواع، المرافق، المعرض، التواصل/التذييل — وثّق أي عنصر مكسور/فارغ/بديل نص بديل (alt) مفقود.
- **محرك البحث:** نطاق صحيح → نتائج توفر حقيقية · نطاق معكوس → رفض واضح · نطاق فارغ/تاريخ ماضٍ → سلوك صحيح.
- **حجز PAY_AT_HOTEL** من البداية للنهاية → مرجع `HTL-2026-…` + تأكيد + طباعة التأكيد.
- **حجز CARD:** عربون 50% مسجَّل (لا بوابة دفع حقيقية — هذا مقرر في الخطة لا خلل) — تحقق من ظهور العربون في الفاتورة لاحقًا.
- **إدارة الحجز:** بحث بالمرجع + الهاتف → عرض · إلغاء وفق سياسة 24 ساعة (اختبر داخل/خارج النافذة) — تحقق من رسائل الحالة الحرفية.
- Idempotency: أرسل الحجز نفسه مرتين بسرعة — لا يُنشأ حجز مزدوج (I7/I8).

### 6.2 بوابة الدخول
- الدخول بكل كود من الأكواد الأربعة → التوجه الصحيح لكل دور.
- **اختبارات سلبية:** كود خاطئ → رسالة الرفض الحرفية · كود فارغ · تكرار المحاولات الخاطئة → rate-limit يعمل.
- الخروج (logout) ثم محاولة الوصول لمسار محمي بالتوكن القديم → رفض.
- Renewal: جلسة تُمدد (عقد §1.2.1: Bearer عمره ≤ 12 ساعة + renew).

### 6.3 وضع الضيف (ادخل بـ `H834729X7`)
لوحة الإقامة (الغرفة/التواريخ/العد التنازلي) · كتالوج الخدمات → **إنشاء طلب** (مع/بدون عاجل) · تفصيل الطلب وإلغاؤه · **المحادثة الفورية** مع الاستقبال (إرسال واستقبال لحظي) · **الفاتورة**: تحقق حسابيًا سطرًا سطرًا (الليالي بسعر اللقطة + الضريبة 15% بتقريب السنت + البنود − المدفوعات = الرصيد) · طلب تمديد · طلب تغيير غرفة · طلب الخروج · الملاحظات (feedback) · الإشعارات + تعليم المقروء.
- **عزل البيانات (I3):** جرّب الوصول لبيانات إقامة ضيف آخر عبر التلاعب بالمعرفات من أدوات المطوّر — يجب الرفض خادميًا.
- ادخل أيضًا بـ `H119922K4` (حالة المغادرة) ووثّق عرض حالتها بتاريخك الفعلي.

### 6.4 وضع الاستقبال (ادخل بـ `R492671M3`)
- اللوحة: KPIs + وصولات اليوم + مغادرات اليوم + نشاط الطلبات.
- **معالج الوصول 4 خطوات:** تحقق الضيف → تعيين غرفة (متاحة من نفس النوع فقط — تحقق أن القائمة مصفّاة) → تأكيد → **كود الضيف يظهر خامًا مرة واحدة** + نسخ + رابط wa.me برقم وجهة قابل للتحرير (قرار W0).
- **المقيمون** → **تفصيل الإقامة (5 تبويبات):** الضيف (لقطة سعر الليالي) · الفاتورة (تسجيل دفعة + إضافة بند — تحقق انعكاسهما على الرصيد فورًا) · الطلبات · الرسائل (محادثة ثنائية الاتجاه) · الإجراءات (قرارات التمديد/تغيير الغرفة: اقبل وارفض كليهما).
- **الطلبات:** الفلاتر الثمانية + العاجل + كل إجراءات الحالة (استلام/إسناد بفريق/تنفيذ/انتظار/إكمال/رفض/إلغاء بتأكيد) — تحقق من انتقال الحالة وخطها الزمني.
- **لوحة الغرف:** التلوين بحسب الحالة + انتقالات (تحتاج تنظيف → متاحة · خارج الخدمة · إعادة) بتأكيدين — وتحقق أن **غرفة مشغولة لا تتحول يدويًا** (I12).
- **معالج الخروج 3 خطوات:** مراجعة → تسوية الرصيد (دفعة سريعة) أو تأكيد مع رصيد متبقٍ (حوار تحذير) → التنفيذ: الغرفة تصبح «تحتاج تنظيف» + **كود الضيف يُبطل فورًا** (جرّب الدخول به بعدها — يجب أن يموت: I11).
- **البحث الفوري** بالمرجع/الاسم/الهاتف/رقم الغرفة (debounce).
- المدفوعات/الرسوم من حواراتها الحرفية.

### 6.5 وضع الإدارة (ادخل بـ `A371849L9`) — الأقسام الأحد عشر كاملة
لوحة التحكم (4 KPIs + توزيع حالات الغرف بألوانه + إيراد 14 يومًا + أحدث الحجوزات + تنبيهات + أكواد نشطة→توليد) · **إعدادات الفندق** (كل الحقول + «أقل إصدار مسموح للتطبيق»: اضبطه ثم تحقق من انعكاسه فورًا في `GET /api/public/app-config`، ثم أفرغه وتحقق من زوال الفرض) · أنواع الغرف (نموذج كامل + حذف مرتبط→تعطيل وحر→نهائي) · الغرف (إنشاء/تعديل — **OCCUPIED غير قابلة للاختيار يدويًا**: حاول عبر أدوات المطوّر إرسالها مباشرة → الخادم يرفض: I12) · المعدلات الموسمية (إنشاء بتداخل → التحذير يظهر **والإنشاء ينجح** — سلوك مقرر لا خلل + الحذف) · الخدمات وأقسامها (CRUD + تفعيل/تعطيل) · **الطاقم والأكواد** (إضافة/تعديل/تعطيل موظف — والتعطيل يبطل كوده وجلساته + توليد كود: موظفون مطابقون للدور فقط + صلاحية 1–30 يومًا + الكود الخام مرة واحدة + النسخ + الإبطال بحوار يذكر الجلسات) · الحجوزات (فلاتر الحالات + بحث + مصفّحة 20/صفحة + تفصيل **بلقطة السعر الحرفية**: الليالي بأسعارها وقت الحجز + الضريبة + سياسة الإلغاء) · الضيوف (بحث + آخر حجز) · التقارير (إشغال 14 يومًا + إيراد 6 أشهر + إحصاءات الطلبات + الجنسيات) · سجل التدقيق (فلترة 24 فعلًا + بحث + مصفّحة 30/صفحة + تفاصيل الأحداث — تحقق أن كل عملية حرجة أجريتها أعلاه دخلت السجل: I10).

### 6.6 الوقت الفوري (اختبار ثنائي الجلسة — إلزامي)
افتح جلستين متزامنتين (متصفح مستقل أو سياق خاص لكل دور):
1. **ضيف ينشئ طلب خدمة → يظهر عند الاستقبال بلا تحديث يدوي** + إشعار/توست.
2. رسالة محادثة ضيف→استقبال ورد الاستقبال→الضيف لحظيًا.
3. تغيير حالة غرفة من الاستقبال → انتقال اللون في لوحة الإدارة/الاستقبال بلا تحديث.
4. حجز جديد من الموقع العام → يصل للإدارة (لوحة/إشعار).
5. تحقق من الاتصال عبر البوابة (`?XTransformPort=3002`) وليس منفذًا مباشرًا.

### 6.7 الألوان والثيم واللغة والاتجاه (عبر كل الأوضاع)
- **الألوان:** العلامة الكحلية `#1A3C6E` والذهبية `#D4A843` حاضرة ومتسقة (رأس، أزرار رئيسية، تمييز) — بلا تسريب ألوان دخيلة (انتبه لأزرق/نيلي دخيل غير مقرر).
- **الثيم:** بدّل ليلي/نهاري في كل وضع — لا نص غير مقروء، لا عناصر تظل بلون واحد، الحفظ بين الجلسات.
- **اللغة:** عربية سليمة بلا نصوص إنجليزية تتسرب للمستخدم (عدا المصطلحات التقنية المقررة) · بلا نصوص مفقودة/مفاتيح ترجمة خام · **جزر LTR**: الأكواد والهواتف والمراجع والمبالغ تُعرض اتجاهًا صحيحًا داخل صفحة RTL.
- **الخط:** Cairo مطبق فعلًا (وليس خط نظام بديل) — تحقق من الشبكة عبر أدوات المتصفح.
- **الاتجاه:** `dir="rtl"` على الجذر، والتخطيطات لا تنكسر.

### 6.8 الاستجابة والتذييل والأفضلية الوصولية
- 375/768/1440: لا فيض أفقي، لا عناصر متداخلة، أهداف لمس ≥ 44px في الجوال.
- **التذييل ملتصق بأسفل الشاشة** في الصفحات القصيرة، ودافَع للأسفل طبيعيًا في الطويلة (لا يغطي محتوى).
- عناصر دلالية (main/header/nav) وبدائل نصية للصور.

### 6.9 المحاسبة — فحص عميق مستقل (أعلى أولوية)
- **الضريبة:** `round(subtotal × 15 / 100)` بالسنت — تحقق على فاتورة حقيقية بحساب يدوي مطبوع في التقرير.
- **العربون CARD:** 50% مضبوط.
- **الرصيد:** `grand + Σ charges − Σ paid` — على إقامة حقيقية.
- **I9 (اللقطة):** أنشئ حجزًا → غيّر سعر النوع من الإدارة → أعد فتح حجزك: **السعر القديم ثابت في الفاتورة والتفصيل**.
- **I1/I7 (البيع فوق المخزون/التزامن):** حاول حجز كل الغرف المتاحة في نطاق ثم حجزًا إضافيًا → الرفض.
- سلبية: لا NaN/undefined/قيم سالبة ظاهرة، لا انهيار عملة في التنسيق.
- **التقارير:** طابق رقمًا واحدًا على الأقل من تقرير الإدارة ضد استعلام مباشر من قاعدة البيانات (SQLite) — أظهر الاستعلام والنتيجة.

### 6.10 انضباط الاختبارات المدمّرة
الكتابة مسموحة (بيانات seed تجريبية): Check-In/Out، دفعات، توليد/إبطال أكواد، تعديل إعدادات — **لا تمس الكود**. عند الانتهاء أعد الحالة الأولى: `bunx prisma migrate reset` (سيُعيد الزرع تلقائيًا عبر seed) ثم أعد تحقق الإقلاع (`/` و`/api/public/hotel`)، ووثّق ذلك في التقرير.

---

## 7) المرحلة الرابعة — الفحص الحي لتطبيق الجوال (Flutter)

**استراتيجية مدرّجة — نفّذ أعلى مستوى متاح لبيئتك ووثّق المستوى المُتبع:**

- **المستوى A (الأمثل):** محاكي/جهاز Android متاح → `flutter run --flavor dev` (ثبّت عنوان الخادم عبر `--dart-define=API_BASE_URL=…` أو من شاشة «إعدادات الخادم» في الدخول). اختبر حيًا: الإطلاق → الدخول بـ H/R/A → أصداف الأدوار الثلاثة (ضيف/استقبال 6 تبويبات/إدارة 11 قسمًا) → إنشاء طلب → الفاتورة → إشعار لحظي (Socket عبر `?XTransformPort=3002`) → **حارس minAppVersion**: اضبط من الإدارة حدًا أعلى من إصدار البناء → التطبيق يحجب بشاشة «يتوفر تحديث مطلوب».
- **المستوى B:** لا محاكي → جرّب `flutter run -d chrome` (تشخيصي فقط — التطبيق مستهدف Android؛ وثّق أي قصور تشغيلي بصدق).
- **المستوى C (الأدنى المقبول):** `flutter analyze` + `flutter test` (169 مرجعية) + فحص سجلات GitHub Actions الأخيرة (mobile-ci) + تحقق من وجود APK موقّع في GitHub Releases (حمّله ووثّق وجوده/توقيعه — التشغيل يتطلب جهازًا فعليًا: إن تعذر، صرّح بذلك).
- **فحص التكافؤ مع الويب (AD-03 — عقد النقل):** قارن شاشة-بشاشة مقابل الويب: التسميات العربية حرفية، ألوان الثيم (`lib/ui/theme.dart` مقابل المتغيرات في الويب)، آلات الحالة نفسها، الفلتر نفسه، أحجام اللمس، واتجاه RTL. أي انحراف تسمية/سلوك بين المنصتين = عيب «تكافؤ نقل» يُسجَّل.
- **جودة كود الجوال:** `flutter analyze` صفر · لا `print` · لا `withOpacity` مهجورة · كل نداء API عبر `ApiClient` (لا HTTP مباشر) — افحص عينات بالبحث في `mobile/lib/`.

---

## 8) مصفوفة الأبعاد الاثني عشر (المطلوبة حرفيًا — كل بُعد بلا استثناء)

| # | البُعد | ما يُفحص | كيف يُختبر حيًا |
|---|---|---|---|
| 1 | **الشاشات** (جوال) | كل شاشة في `mobile/lib/screens/**` لكل الأدوار الثلاثة | تشغيل (مستوى A/B) أو تحليل + مقارنة حرفية مع نظيرتها في الويب + لقطات |
| 2 | **الصفحات** (ويب) | كل أوضاع/مقاطع SPA: الموقع العام بأقسامه، الضيف، الاستقبال بتبويباته، الإدارة بأقسامها الـ 11 | متصفح آلي — زيارة فعلكة لكل قسم + لقطة + أخطاء console |
| 3 | **الوظائف** | كل زر/نموذج/حوار/فلتر/بحث/نسخ/طباعة يفعل ما هو له | نقرة فعلية لكل وظيفة أساسية + نتيجة مرصودة |
| 4 | **العمليات** | دورة الحياة كاملة: حجز → وصول → إقامة (خدمات/رسائل/تمديد/تغيير) → خروج → تنظيف → جاهزية | تنفيذ سلسلة كاملة حية من طرفين (زائر+استقبال+ضيف) |
| 5 | **المتطلبات** | وثيقتا upload/PLAN_* + §2.1 من MASTER_PLAN | جدول تتبع: متطلب → مكانه → منفَّذ/غائب/منحرف |
| 6 | **الاعتمادات** | package.json + pubspec.yaml + إصدارات متناقضة + حزم مهملة + مخاطر معروفة | مراجعة + بحث ويب عند الشك |
| 7 | **الألوان** | القيم المقررة `#1A3C6E`/`#D4A843` وتطبيقها وتسرب ألوان دخيلة | لقطات + فحص بالعين/بأداة فهم الصور + قراءة متغيرات الثيم في الكود |
| 8 | **الثيم** | ليلي/نهاري في الويب والجوال + تباين القراءة + ثبات الاختيار | تبديل فعلي في كل وضع + لقطات قبل/بعد |
| 9 | **اللغة** | عربية سليمة، صفر تسرب إنجليزي، جزر LTR، خط Cairo | تفحص بصري لكل شاشة + بحث في الكود عن نصوص مستخدم صريحة غير عربية |
| 10 | **المنطق** | آلات الحالة، الحالات الحدية، التحقق من المدخلات، رسائل الأخطاء، تجاوب النماذج | اختبارات سلبية مقصودة (قيم فارغة/حدود/حقن بسيطة في النماذج) |
| 11 | **المحاسبة** | كل قواعد المال (§6.9) + التقارير + الثوابت المالية I2/I8/I9 | حسابات يدوية مطبوعة + استعلام قاعدة مباشر + تجارب التداخل |
| 12 | **الأقسام** | اكتمال كل قسم: موقع (6 مقاطع) · ضيف (كل اللوحات) · استقبال (6 تبويبات + تفاصيلها) · إدارة (11 قسمًا) | زيارة موثقة بجرد لكل قسم: موجود/ناقص/معطوب |

> **قاعدة الإغلاق:** التقرير لا يُقبل وفيه بُعد واحد فارغ أو بلا أدلة. إن تعذر اختبار بُعد حيًا كليًا (غياب محاكي مثلًا)، يسجَّل في مصفوفة التغطية بصراحة مع المستوى البديل المُتبع.

---

## 9) إلزامية استعمال كافة القدرات والمهارات

- **كل أدوات بيئتك مطلوبة، لا خيار:** قراءة/بحث الملفات، تنفيذ الأوامر، **متصفح آلي بلقطات شاشة**، **تحليل الصور (فهم بصري) على اللقطات** للتحقق من الألوان/الثيم/التماسك البصري، **بحث الويب** عند فحص مخاطر الاعتماديات، وأدوات توليد المستندات للتقرير النهائي.
- **إن كانت بيئتك تدعم وكلاء فرعيين:** شغّل الأبعاد في **موجات متوازية** (مثلًا: 6.1–6.2، 6.3، 6.4، 6.5، 6.6–6.8، 7، 8/5–6/9–11) بضوابط نجاح لكل موجة مأخوذة من هذه الوثيقة حرفيًا، ثم موجة تكامل تضم النتائج. لا تسمح لأي وكيل بتجاوز قواعد الأدلة.
- **ممنوع الاكتفاء بالقراءة النظرية** — طلب المالك الصريح: عمل فعلي من كل النواحي.

---

## 10) سجل العيوب — الحقول الإلزامية لكل عيب

| الحقل | الوصف |
|---|---|
| ID | `AUD-001` تسلسليًا |
| الخطورة | CRITICAL / MAJOR / MINOR / COSMETIC / INFO (القسم 11) |
| البُعد | واحد أو أكثر من الأبعاد الاثني عشر |
| المنصة | web / mobile / server / docs / cross |
| الموضع | شاشة/صفحة + المسار، أو `ملف:سطر` |
| العنوان | وصف دقيق سطر واحد |
| المتوقع | المرجع الحرفي (من الخطة/العقد/سلوك الويب المناظر) |
| الفعلي | ما رُصد حيًا |
| خطوات الاستنساخ | مرقّمة قابلة للتنفيذ من طرف ثالث |
| الدليل | مرجع الملحق (لقطة/مخرج أمر/سطر سجل) |
| سبب جذر مُرجَّح | تحليل تقني (يوسم «مُرجَّح» صراحة) |
| الإصلاح المقترح | توصية محددة — **بلا تنفيذ** (النطاق قراءة/تدقيق) |

---

## 11) تصنيف الخطورة

- **CRITICAL:** مس مال (تسعير/رصيد/عربون/فاتورة)، مصادقة/صلاحيات، بيانات (عزل/فقد)، كسر ثابت من I1–I12، انهيار خدمة/صفحة كاملة.
- **MAJOR:** وظيفة معطوبة أو معطّلة، تدفق محجوب، متطلب مخطط غائب، عقد API منكسر.
- **MINOR:** حالة حدية فاشلة، تناقض بيانات/سلوك غير مالي، تجاوب محدود.
- **COSMETIC:** لون/تباعد/أيقونة/إملاء — لا يمنع الاستخدام.
- **INFO:** ملاحظات وتحسينات (انزياح تواريخ seed مثلًا).
- فئة إضافية مستقلة: **INV-كسر** لأي انتهاك لثابت من I1–I12 (يرافقها الخطورة العليا تلقائيًا).

---

## 12) صيغة التقرير النهائي (المخرج الوحيد المطلوب)

1. **الغلاف التنفيذي:** هوية المهمة + تاريخ/بيئة التنفيذ + الحكم الكلي في ≤ 5 أسطر + جدول الإصابات (خطورة × عدد).
2. **الفهم المستخلص:** أجوبة أسئلة المعايرة الاثني عشر (بعباراتك من المستودع).
3. **التجهيز والبيئة:** كل أمر بمخرجه الحرفي + نقطة الوقوف (commit) + أي تصحيح بيئة اقتضاه.
4. **الفحص الساكن:** جدول S1–S8 بنتائجه الحرفية.
5. **الفحص الحي — الويب:** حسب 6.1→6.10 لكل وضع: ما فُحص، ما رُصد، الأدلة.
6. **الفحص الحي — الجوال:** المستوى المُتبع (A/B/C) + النتائج + فحص التكافؤ مع الويب.
7. **الفحص الحي — الوقت الفوري:** نتائج الاختبار الثنائي الخمس.
8. **سجل العيوب الكامل:** بترتيب الخطورة ثم البُعد (حقول القسم 10 كاملة).
9. **مصفوفة التغطية:** 12 بُعدًا × (ويب/جوال/خادم) × عدد الفحوص × عدد العيوب × الحالة (مغطى/مغطى جزئيًا/غير قابل للتغطية + السبب).
10. **تتبع المتطلبات:** جدول المتطلب → الحالة (من S7 ومن الفحص الحي).
11. **فحص الثوابت I1–I12:** ثابت-ثابت: كيف اختُبر حيًا + النتيجة.
12. **التوصيات المرتبة:** بلا تنفيذ — قائمة أولويات إصلاح مقترحة.
13. **إقرار النزاهة:** ما نُفّذ فعلًا، ما لم يمكن التحقق منه ولماذا، حدود البيئة، وبيان استعادة حالة قاعدة البيانات بعد الاختبارات المدمّرة.
14. **ملحق الأدلة:** كل اللقطات والمخرجات والسجلات المرقّمة (`EV-001`…) التي يستند إليها السجل.

---

## 13) قواعد السلوك والحدود (Guardrails)

1. **قراءة فقط على كود المشروع:** لا تعديل ملف، لا commit، لا push، لا فتح Pull Request. الاستثناء الوحيد: ملفات البيئة التي تولّدها أنت (`.env`، ملف القاعدة) وملفات عملك الخاصة (لقطات/تقارير خارج شجرة المشروع أو غير متتبعة).
2. **لا تلمس GitHub:** لا تطلق Actions (رصد سجلها فقط)، لا تُصدر Releases، لا تعدل إعدادات المستودع.
3. **الكتابة الحية** (بيانات) مسموحة على بيانات seed فقط — مع الاستعادة الموثقة (§6.10) في النهاية.
4. **استقلالية كاملة:** لا تسأل المالك شيئًا؛ عند العائق: وثّق، تحايل بما لا يضر، وواصل.
5. **الصدق فوق الاكتمال:** إن لم تستطع فحص شيء، قُل ذلك صريحًا — عدم القدرة الموثّق أفضل من ادعاء فارغ. تذكّر: التاريخ عاقب من قبل ادّعى الفحص دون فعله.
6. **لا إصلاحات:** حتى لو وجدت خللًا بسيطًا — تُسجّل توصيته فقط. فتح المنطق المقفل قرار مالك لا قرار مدقق (AD-06).

---

## 14) معايير قبول المهمة كاملة (Checklist نهائي)

- [ ] البيئة مرفوعة بالكامل (ويب 3000 + realtime 3002/3004 + قاعدة مبنية بـ migrate + seed) بمخرجات موثقة.
- [ ] بوابة المعايرة مُجابة من المستودع.
- [ ] S1–S8 منفذة بمخرجات حرفية.
- [ ] فحص الويب الحي: 6.1→6.10 كلها منفذة أو مُوثّق عذرها الصريح.
- [ ] فحص الجوال: أعلى مستوى متاح + فحص التكافؤ.
- [ ] الاختبار الثنائي للوقت الفوري خمس حالاته.
- [ ] مصفوفة الأبعاد الاثني عشر: لا بُعد فارغ.
- [ ] فحص الثوابت I1–I12 ثابتًا-ثابتًا.
- [ ] سجل عيوب مكتمل الحقول بأدلة مرقّعة.
- [ ] التقرير بهيكل القسم 12 كاملًا + إقرار النزاهة + استعادة الحالة.
- [ ] صفر ادعاء بلا دليل. صفر «يبدو/أظن/على الأرجح».

---

## ملحق أ — ورقة الأوامر الجاهزة

```bash
# ويب
bun install
cp .env.example .env            # ثم DATABASE_URL="file:/ABS/PATH/db/custom.db"
bunx prisma generate && bunx prisma migrate deploy && bunx prisma migrate status
bun prisma/seed.ts
bun run dev                     # 3000
bun run lint
bun run test                    # 165 مرجعية

# الوقت الفوري (طرفية مستقلة)
cd mini-services/realtime && bun install && bun run dev   # 3002 + 3004

# جوال (إن توفر SDK)
cd mobile && flutter pub get && flutter analyze && flutter test   # 169 مرجعية
flutter run --flavor dev --dart-define=APP_ENV=dev

# استعادة بيانات العرض بعد الاختبارات المدمّرة
bunx prisma migrate reset
```

## ملحق ب — ملاحظات معايرة (أمثلة مثبتة من مُعِدّ هذه الوثيقة — نموذج مستوى الدليل المطلوب منك)

المُعِدّ تحقق فعليًا من المستودع قبل الإصدار، ورصد انحرافات توثيقية حقيقية تُسلَّم لك كعينات معايرة لا كإعفاء من البحث — ابحث عن نظائرها في كل مكان:

1. **عنوان الخلاصة متأخر:** السطر الأول في `MASTER_PLAN.md` ما زال يقول «v2.4» بينما سجل التغييرات §14 يوثق إصدار 2.5 (2026-09-04). (دليل: `MASTER_PLAN.md:1` مقابل الصفوف الأخيرة من §14.)
2. **عدد اختبارات الويب في README الجذر قديم:** القسم «الاختبارات» يقول «157 اختبارًا» بينما العدد الأخضر الموثق في worklog والخطة v2.3 هو **165**.
3. **عدد النماذج في README الجذر:** §5 البنية يقول «20 نموذجًا» بينما المخطط الفعلي والخطة يوثقان **22 نموذجًا**.

هذه عيوب من فئة COSMETIC/INFO (توثيق فقط) — لكنها تثبت أن التدقيق الحرفي بالموقع `ملف:سطر` يكشف ما تخفيه القراءة السريعة. **مطلوبك أعلى بكثير: أن تُنزل هذا المعيار على الشاشات والعمليات والمحاسبة حيًا.**

---

**نهاية وثيقة المطالبة — أي عمل خارج حدودها أو دون معاييرها يُعد عدم إنجاز.**
