# Prisma — SQLite database connection
# المسار النسبي يُحلّ بالنسبة لمجلد prisma/ (موقع schema.prisma)
# هذا الملف نموذج فقط — انسخه إلى .env ثم عدّل المسار لمسار مشروعك المطلق إن لزم
DATABASE_URL="file:../db/custom.db"
