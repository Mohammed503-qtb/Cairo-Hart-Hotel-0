# Task 21-c — Report (written by Main Agent after independent review)

> ملاحظة: تقرير الوكيل الأصلي فُقد بمهلة نقل بعد كتابة ملفاته
> (نفس ظاهرة 20-a في Task 20 و 21-a). هذا التقرير أعاده الوكيل الرئيسي
> بعد مراجعة تكاملية مستقلة لكل ملف أنتجه. قسم 21-c في worklog.md
> غائب أيضًا — الضمان هنا.

## Scope delivered
3 شاشات F5 (الإعدادات + الخدمات + الحجوزات):

- `mobile/lib/screens/admin/hotel_settings_screen.dart` (695 سطرًا تقريبًا)
  - نموذج الحقول الحرفية الكاملة من hotel-settings.tsx: الاسم/tagline/الوصف/
    الهاتف/واتساب/البريد/العنوان/المدينة/العملة/أوقات HH:MM (تحقق عميلي
    regex + رسائل الخادم الحرفية عند ApiError)/الضريبة 0-100/زيادة نهاية
    الأسبوع/حدود الإقامة 1-30 و 1-60/أفق الحجز 1-730/السياسات الخمس +
    **minAppVersion** (x.y.z أو فارغ — حقل F6)
  - الحفظ: بناء payload بالحقول (الأرقام int) → `store.updateHotel(payload)`
    → toast بnote الخادم الحرفية («تغيير الضريبة والأسعار يؤثر على الحجوزات
    الجديدة فقط…») — مراجعة بعيني: الأسطر 37-213
- `mobile/lib/screens/admin/services_screen.dart` (1036)
  - قسمان كالويب: **أقسام الخدمات** (بطاقة: الاسم/المفتاح/الأيقونة/الترتيب/
    عدد الخدمات + إضافة/تعديل/حذف بتأكيد — DELETE يرفضه الخادم إن وُجدت خدمات:
    رسالته الحرفية تظهر) + **الخدمات** (بطاقة: الاسم/السعر MoneyText/القسم/
    فعال + إضافة/تعديل/حذف بتأكيد)
  - الجسد الحرفي: خدمة `{name, nameEn, description, priceCents, active,
    sortOrder, categoryId}` (خط 732-736) وقسم `{name, nameEn, key, icon,
    sortOrder}` (خط 917-920) — مطابق لمسارات API ✓
- `mobile/lib/screens/admin/reservations_screen.dart` (1104)
  - فلتر الحالات السبع + بحث (مرجع/اسم/هاتف) + مصفّح 20/صفحة عبر
    `store.refreshReservations(status,q,page)` — الفلاتر تقيم في المخزن
  - بطاقة حجز: المرجع RefCodeText/الضيف/النوع/التواريخ والليالي/الإجمالي
    والمدفوع/StatusChip.reservationStatus + paymentStatus + المصدر
  - التفصيل صفحة كاملة `_ReservationDetailPage` عبر
    `store.loadReservationDetail(id)` (بلا تخزين مؤقت — نمط R-05): كل حقول
    A-30 + **priceSnapshot معروضة حرفيًا** (ليالٍ بسعر كل ليلة
    `_snapshotNights` + الضريبة + سياسة الإلغاء وقت الحجز — مراجعة بعيني:
    الأسطر 740-920) + الدفعات + الإقامة إن وُجدت

## Tests
- `mobile/test/screens/admin/hotel_settings_test.dart` (5 testWidgets)
- `mobile/test/screens/admin/reservations_test.dart` (5)
- `mobile/test/screens/admin/services_test.dart` (5)
- تغطي: جسم PATCH بأسماء الحقول الحرفية + note الخادم يظهر + قائمة الحجوزات
  بالفلتر في الاستعلام (status=&q=&page=) + التفصيل يعرض لقطة السعر من Mock +
  إنشاء خدمة بجسم بالسنت الصحيح

## Independent review by Main Agent (verified, not trusted)
- توازن الأقواس برمجيًا: متوازنة ✓
- الأيقونات grep ضد icons.dart الرسمي: كلها موجودة ✓
- نداءات store مطابقة للتوقيعات (فحص برمجي) ✓
- العقود المجمدة: `HotelSettingsScreen/ServicesScreen/AdminReservationsScreen`
  كلها `{super.key, required this.store}` كما تستوردها admin_shell.dart ✓
- صفر print / withOpacity / HTTP مباشر ✓
- المال بالسنت: الإرسال int (×100 round) والعرض fmt.formatMoney ✓

## Risks
1. افتراض توقيت UTC في اختبارات التواريخ (نمط مجرَّب)
2. textContaining على النصوص المدمجة (نمط مجرَّب أخضر في CI Task 19/20)
