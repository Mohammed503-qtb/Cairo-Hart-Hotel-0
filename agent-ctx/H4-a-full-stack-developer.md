# H4-a — full-stack-developer — جرد العقود الكامل docs/CONTRACTS.md v0.9

**Task ID:** H4-a · **المهمة:** H4.1 من MASTER_PLAN v2.0 — جرد كل مسارات API المنفَّذة كما هي في الكود في `docs/CONTRACTS.md`.

## الملكية والحدود
- ملكيتي الحصرية: `docs/CONTRACTS.md` فقط — لم أعدل أي ملف آخر (prisma/ لوكيل H1، tests/ لوكيل H2-a).
- قارئ وموثق فقط: لا تغيير سلوك، لا build، لا قتل للخادم.

## ما فعلته
1. قرأت worklog.md بالكامل وMASTER_PLAN.md (§5 H4 + قاعدة الاستقرار + §12.5 سطح API).
2. قرأت سطرًا بسطر: كل ملفات المسارات الـ 67 تحت src/app/api (بما فيها `_lib/_shared/_helpers` المشتركة) + src/lib/{api,auth,rate-limit,audit,events,pricing,availability,codes}.ts + seed.ts (قراءة فقط لاستخراج قيم أمثلة واقعية: ضريبة 15%، زيادة نهاية أسبوع 10%، أسعار الأنواع 8000/12000/16000/22000 سنتًا).
3. عددت فعليًا بالأدوات: 67 ملف مسار (6 public + 2 auth + 14 guest + 22 reception + 22 admin + 1 جذر) = **82 نقطة نهاية** (6+2+16+23+34+1). الخطة كانت تقول 57 — الانحراف موثق.
4. كتبت docs/CONTRACTS.md v0.9 (~1865 سطرًا، 11 قسمًا): مقدمة (مغلف ok/fail، نموذج المصادقة الفعلي Bearer — لا كوكي، placeholder عقد جوال H3، دلالات الحالات، حدود المعدل، أشكال مشتركة، Enums) + بطاقة لكل نقطة نهاية (الدور/الطلب بالتحقق/النجاح حقليًا بمثال بالسنت/الأخطاء برسائلها العربية الحرفية/الآثار الجانبية: audit+socket+notifications/المستهلك W·F1·F4·F5/الحالة DRAFT) + حزمة F1 (16 نقطة أساسية + مكملات + 7 قواعد عقدية) + قاعدة الاستقرار H4.2 بجدول وسم فارغ + ملاحظات انحراف مرقمة (11) + التحقق الذاتي.
5. تحقق حي بـ curl (قراءة/أخطاء غير مكتوبة فقط — صفر كتابة بيانات): /api/public/hotel (503 حرفية — حالة بيانات H1 لا كود)، /api (Hello world)، 5 مسارات المراجعة العشوائية أدناه.
6. المراجعة العشوائية الإلزامية (5 مسارات من 5 قنوات × قراءتين): validate، bookings، guest/bill، reception/rooms/[id]/status، admin/codes — **صفر انحراف** (النتائج الحية مطابقة حرفيًا للرسائل).

## أبرز الاكتشافات (انحرافات خطة↔كود، موثقة §10)
- العد الفعلي 67 ملفًا/82 نقطة (الخطة: 57). guest فعليًا 14 ملفًا (الخطة 15)، reception/admin فعليًا 22/22 (الخطة 17/17).
- المصادقة اليوم **Bearer token في Authorization** يعود في جسم validate ويُخزن العميل (zustand persist) — لا كوكي إطلاقًا؛ عكس وصف «كوكي الجلسة». placeholder عقد H3 جاهز في §1.2.1.
- `GET /api` بقايا سقالة خارج مغلف ok/fail — مرشح للحذف بقرار.
- غرفة socket `admin` معرفة بلا أي بث إليها؛ حالة `USED` مقبولة كفلتر أكواد ولا تُضبط أبدًا؛ `details` في audit تُعاد نص JSON خام.
- تفاوتات عدّ دقيقة: departuresToday سقف 5؛ unreadCount داخل العينة (5/50/30)؛ 201/200 غير متسق (رسائل ضيف 201 مقابل استقبال 200).
- حالة حية وقت الجرد: db/custom.db (عمل H1) بلا سجل Hotel → public hotel/room-types تعيد 503 بالرسالة الحرفية — مسار الكود سليم.

## النتائج
- docs/CONTRACTS.md v0.9 مكتمل: تغطية 100% لملفات المسارات، حزمة F1 مجمعة، قاعدة الاستقرار منقولة، المراجعة الخماسية صفر انحراف.
- مرجع F0/F1 جاهز: كل ما يحتاجه تطبيق الضيف موجود في §8 بقواعد عقدية (Bearer، موت الجلسة اللحظي، 403 الكيان مقابل 403 الصلاحية، سنت صحيح، socket اختياري عبر /?XTransformPort=3002).
