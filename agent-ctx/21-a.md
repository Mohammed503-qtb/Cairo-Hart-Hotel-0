# Task 21-a — Report (written by Main Agent after independent review)

> ملاحظة: تقرير الوكيل الأصلي فُقد بمهلة نقل بعد كتابة ملفاته وقسمه في worklog.md
> (نفس ظاهرة 20-a في Task 20). هذا التقرير أعاده الوكيل الرئيسي بعد مراجعة
> تكاملية مستقلة لكل ملف أنتجه.

## Scope delivered
4 شاشات F5 الناقصة (القبول الحرفي للمرحلة — توليد/إبطال كود + مراجعة تدقيق):

- `mobile/lib/screens/admin/staff_codes_screen.dart` (1417 سطرًا)
  - قسمان بـ SegmentedButton (الأكواد افتراضيًا كالويب)
  - الأكواد: فلاتر النوع/الحالة عبر `store.refreshCodes(type:,status:)` — القيم
    تقيم في المخزن + بطاقة كود: codeMasked LTR monospace / النوع / الحالة /
    السياق (موظف أو ضيف+غرفة أو مرجع إقامة) / الانتهاء / آخر استخدام timeAgoAr
    أو «لم يُستخدم» / زر إبطال للفعّال فقط (الملغي بعتامة 0.55)
  - حوار التوليد: موظفون فعّالون **مطابقون للدور فقط** (استقبال→RECEPTION ·
    إدارة/مدير→ADMIN — `eligibleStaff` كالويب) + تحذير الكود الفعّال الحالي +
    صلاحية 1-30 افتراضي 7 → `store.generateCode(type, staffId, days)` →
    `RawCodeBox(code)` (الخام مرة واحدة) + «توليد آخر» + إغلاق
  - حوار الإبطال يذكر الجلسات حرفيًا → `store.revokeCode(id)` → توست رسالة
    الخادم الحرفية
  - الطاقم: بطاقة (الاسم/شارة الدور/الهاتف/سويتش/تعديل + صندوق «أحدث كود») +
    إضافة `{fullName,role,phone}` بtrim + تعديل `{fullName,phone}` + تعطيل
    بحوار تأكيد يذكر إبطال الكود والجلسات ثم `PATCH {active:false}`
- `mobile/lib/screens/admin/audit_log_screen.dart` (570)
  - فلتر إجراء بكل الأفعال الـ24 الحرفية من الويب + بحث حر + مصفّح 30/صفحة
    (السابق/صفحة X من Y/التالي) عبر `store.refreshAudit(action,q,page)`
  - بطاقة سجل: chip الإجراء بلون دور الفاعل + entityType + entityId مقتطع +
    details خريطة «مفتاح: قيمة» + timeAgoAr
- `mobile/lib/screens/admin/guests_screen.dart` (382)
  - بحث (اسم/هاتف/بريد) عبر `store.refreshGuests(q)` + بطاقة ضيف كاملة + آخر
    حجز بشارة `StatusChip.reservationStatus` — كل فارغ «—» بأمان
  - حوار حجوزات الضيف (من الويب) استُبعد موثقًا: لا نداء مخزن مخصص له
- `mobile/lib/screens/admin/reports_screen.dart` (501)
  - الإشغال 14 يومًا (`AdminBarChart` بنسبة فوق كل عمود + «المحسوبة على N غرفة
    فعّالة») + الإيراد 6 أشهر (ذهبية بfmt.formatMoney في valueFormatter) +
    طلبات الخدمة (صناديق + byStatus chips + أعلى الخدمات بأشرطة) + الجنسيات

## Tests
- `mobile/test/screens/admin/staff_codes_test.dart` (8 testWidgets)
- `mobile/test/screens/admin/audit_test.dart` (7: تدقيق 4 + ضيوف 1 + تقارير 2)
- تغطي: جسم POST الحرفي {'type','staffId','days'} + ظهور الكود الخام find.text +
  إبطال بحوار تأكيد بجسم {'codeId'} + فلاتر الأكواد في استعلام GET حرفيًا +
  صفحة التدقيق page=2 + المعطّل/غير المطابق غير قابلين للاختيار + رفض تطابق
  الدور برسالة الخادم + إضافة موظف + تعطيل {active:false} + فلتر الإجراء +
  الضيوف فراغ آمن + التقارير (50%/$3,170.00/متوسط null → «—»)

## Independent review by Main Agent (verified, not trusted)
- توازن الأقواس برمجيًا (واعٍ بالنصوص): متوازنة ✓
- كل الأيقونات grep ضد icons.dart الرسمي: كلها موجودة ✓
- كل نداءات store مطابقة للتوقيعات (فحص برمجي لكل store.*) ✓
- العقود المجمدة: `StaffCodesScreen/AuditLogScreen/GuestsScreen/ReportsScreen`
  كلها `StatefulWidget` بـ `{super.key, required this.store}` كما تستوردها
  admin_shell.dart ✓
- صفر print / withOpacity / HTTP مباشر ✓
- جسد حوار التوليد: الموظفون المطابقون للدور فقط + RawCodeBox + ApiError
  برسالة حرفية — قرأت الكود بعيني (الأسطر 890-1000) ✓

## Risks documented
1. افتراض توقيت UTC في اختبارات التواريخ (نمط الزملاء — CI أخضر سابقًا)
2. textContaining على Text.rich في تفاصيل التدقيق (نمط مجرَّب أخضر في Task 20)
3. مطابقة عناصر القائمة المنسدلة بـ .last عند ازدواج النص (نمط الزملاء)
