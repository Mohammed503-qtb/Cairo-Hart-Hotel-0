# MASTER PLAN — v2.4 (خط السير الحي)
**فندق قلب القاهرة — عدن | Cairo Heart Hotel — Aden**

| البند | القيمة |
|---|---|
| نوع الوثيقة | خطة خط سير تنفيذية (Living Route Map) — تُحدَّث مع كل بوابة تُقفل |
| الإصدار | **2.5** — يعلو على كل ما سبقه (سجل التغييرات §15 يوثق 2.0→2.5) |
| أصل هذا الإصدار | الواقع الفعلي المنفَّذ والمتحقَّق منه + جلستا قرار هندسيتين مع المالك (جلسة الاتجاه العام، وجلسة بوابة Flutter) |
| الوثائق التي يعلو عليها | `MASTER_PLAN.md` v1.0 (مؤرشفة في `docs/archive/MASTER_PLAN_v1.md`) و`upload/PLAN_WEBSITE.md` و`upload/PLAN_ MOBILE-APK.md` |
| ما يبقى صالحًا من v1.0 | نموذج الدومين وثوابته وآلات الحالة واصطلاحات التسمية — منقول إلى الملحق المرجعي هنا (القسم 12) |
| اللغة الأساسية | العربية (RTL) — الإنجليزية ثانوية لاحقًا |
| العملة | USD — كل المبالغ سنت صحيح (Int) بلا استثناء |
| المنطقة الزمنية | Asia/Aden — عدد الليالي من حدود الأيام لا الطوابع الزمنية |

> **طبيعة هذه الوثيقة:** ليست وثيقة رؤية ولا حلمًا معماريًا. هي **خط سير محسوب** من نقطة الوقوف الحالية إلى ما بعد الإنتاج، مبني على قرارين متخذين بوعي: (1) لا إعادة كتابة ولا إعادة بناء معماري بلا محفّز واقعي، و(2) الخيار A — تحصين قصير مغلق القائمة قبل Flutter، لأن خطأ التحصين الزائد مرئي وقابل للكبح، بينما خطأ البدء المبكر مخفي بطبيعته ويظهر في يد ضيف حقيقي.

---

## 1. أسبقية القرارات وقواعد القراءة

### 1.1 دلالات الإلزام

| المصطلح | الدلالة |
|---|---|
| **MUST** | إلزامي. عدم تنفيذه = المرحلة غير مكتملة وبوابتها لا تُقفل |
| **SHOULD** | يُنفَّذ افتراضيًا. الاستثناء يُسجَّل في سجل القرارات مع السبب |
| **MAY** | تقديري بشرط عدم خرق أي MUST |
| **MUST NOT** | محظور. خرقه = فشل مراجعة وإعادة عمل |

### 1.2 قواعد أسبقية

1. هذه الوثيقة v2.0 هي **الأعلى أسبقية** عند أي تعارض مع v1.0 أو الخطين الأصليين.
2. عند تعارض متطلب مع قيد واقعي (مزود دفع لا يدعم ميزة مثلًا) يُسجَّل القرار البديل في القسم 3 (سجل القرارات) قبل التنفيذ.
3. الكود المُنفَّذ والمتحقَّق منه هو مصدر الحقيقة لما هو «موجود»؛ هذه الخطة هي مصدر الحقيقة لما هو «قادم».
4. **MUST NOT** يُبتكر سلوك تجاري غير منصوص (أسعار، سياسات، حالات، صلاحيات) «لإكمال شاشة».

### 1.3 قواعد الانضباط أثناء التنفيذ (كيف نمنع الانزلاق إلى إعادة البناء)

- **القائمة المغلقة:** نطاق المرحلة H هو بنودها الأربعة حرفيًا. أي بند خامس يقترح نفسه أثناء التنفيذ، مكانه الافتراضي هو «بعد Flutter» حتى يثبت بالدليل أنه بوابة عبور حقيقية.
- **سقف زمني:** المرحلة H سقفها **أسبوع عمل واحد**. تجاوزه إشارة إنذار تُستعرض مع المالك فورًا.
- **عبارة «بينما نحن هنا…» محظورة (MUST NOT):** هي الطريق المعروف من تحصين قصير إلى إعادة بناء مفتوحة.
- **البوابات لا تُفتح قبل قفلها:** لا يبدأ عمل في مرحلة قبل قفل بوابة المرحلة السابقة — باستثناء واحد صريح: W0 (الاعتماد التجاري لواتساب ذيله طويل، يُسمح ببدئه بمجرد انطلاق F).
- **كل تغيير مخطط قاعدة بيانات = ملف migration جديد (MUST):** بعد قفل H1 لا عودة إلى `db:push` في أي قاعدة ببيانات حقيقية.
- **كل خطوة تُقفل بقبولها المكتوب لا بـ«تمت»:** دليل القبول هو شرط القفل.

---

## 2. الوضع الحالي — رأس المال المُثبَت (نقطة الوقوف)

> هذا جرد الواقع كما هو، لا كما نتمنى. كل ما هنا «متحقق منه» يعني: مُجرَّب فعليًا من طرف إلى طرف (API بالموجهات + الواجهات بالمتصفح)، وليس «كود موجود يُفترض أنه يعمل».

### 2.1 مبني ومتحقق منه ✓

| النطاق | الحالة |
|---|---|
| **الدومين** | فصل Reservation (الوعد) عن Stay (الحقيقة)؛ البيع على Room Type والتخصيص عند الوصول؛ لقطة السعر المجمدة وقت الحجز؛ المال بالسنت الصحيح |
| **قاعدة البيانات** | Prisma + SQLite، **22 نموذجًا**، المخطط في `prisma/schema.prisma` — **على خط migrations رسمي** (baseline مُطبَّق + `migrate deploy` هو المسار الرسمي — قُفلت H1) |
| **الموقع العام + محرك الحجز** | بحث توفر، عرض الأنواع، حجز PAY_AT_HOTEL وCARD (عربون 50% مسجَّل لا بوابة حقيقية)، Idempotency، إدارة/إلغاء حجز، طباعة تأكيد |
| **تطبيق الضيف (ويب)** | دخول بالكود، لوحة الإقامة، طلبات الخدمة بدورة حياتها، محادثة فورية، فاتورة، تمديد، تغيير غرفة، طلب خروج، ملاحظات، إشعارات |
| **تطبيق الضيف (Flutter — mobile/)** | ✅ **F0+F1+F2 منفَّذة 2026-09-02**: مشروع Flutter كامل (RTL + Cairo + كحلي/ذهبي) بشاشات منقولة شاشة-بشاشة من الويب + عميل API بعقد §1.2.1 (401→renew) + Socket.IO — يُبنى عبر **GitHub Actions** (CI خضراء + APK موقّع في Releases) — البناء والتشغيل على GitHub بقرار المالك |
| **وضع الاستقبال** | معالج وصول 4 خطوات (تعيين غرفة + توليد كود ضيف يظهر خامًا مرة واحدة)، خروج بفحص الرصيد، الطلبات، لوحة الغرف، مدفوعات/رسوم، بحث فوري، تمديد/تغيير غرفة |
| **وضع الإدارة** | لوحة KPIs، إعدادات الفندق، أنواع/غرف/معدلات/خدمات، الطاقم وتوليد/إبطال الأكواد، الحجوزات بلقطة السعر، الضيوف، التقارير، سجل التدقيق |
| **الهوية والأمان المطبق** | أكواد H/R/A مُهاشَة SHA-256، جلسات، RBAC مفروض خادميًا في كل مسار، rate-limit على التحقق، سجل تدقيق شامل، عزل بيانات الضيف |
| **الوقت الفوري** | Socket.IO (خدمة مستقلة عبر البوابة) + بث داخلي HTTP — مُتحقق: طلب ضيف يظهر في لوحة الاستقبال لحظيًا |
| **التقديم** | SPA بمسار واحد يبدّل الأوضاع (website / login / guest / reception / admin) — عربي RTL بخط Cairo، ليلي/نهاري، متجاوب موبايل |
| **المستودع** | مرفوع إلى GitHub (`main`) مع README ووثائق — **لا أسرار ولا قواعس بيانات في المستودع** |

### 2.2 غائب أو مؤجل عمدًا (معلوم ومُقرّر)

| الغياب | تصنيفه في هذه الخطة |
|---|---|
| Migrations | ~~بوابة عبور — المرحلة H1~~ ✅ **مقفلة 2026-09-02** — baseline + تعليم القاعدة + مسار نظيف مجرّب |
| اختبارات آلية (خصوصًا مسارات المال) | ~~بوابة عبور — H2~~ ✅ **مقفلة 2026-09-02** — 157 اختبارًا بأمر واحد `bun run test` |
| عقد مصادقة بعمر/تجديد للعميل المحمول | ~~بوابة عبور — H3~~ ✅ **مقفلة 2026-09-02** — Bearer موحّد أصلًا + `renew` + اختبارات |
| وثيقة العقد المستقر للـ API | ~~بوابة عبور — H4~~ ✅ **مقفلة 2026-09-02** — CONTRACTS.md v1.0 ومجموعة F1 معلَنة STABLE |
| Flutter (وضع الضيف) | ~~المرحلة F — بعد بوابة الثقة~~ ✅ **F0/F1/F2 منفَّذة 2026-09-02** (وضع الضيف كاملاً في mobile/ مع CI+Release) · F6 مكتملة نهائيًا · ✅ **F4 منفَّذة 2026-09-03** (F4-a مسار الوصول/الخروج — Task 19 · F4-b المقيمون/تفصيل الإقامة/الطلبات/الغرف/البحث — Task 20) · ✅ **F5 منفَّذة 2026-09-04** (الإدارة كاملة — Task 21) |
| WhatsApp (صادر/وارد) | ~~المرحلة W — مسار أعمال يبدأ مع F~~ **قرار المالك 2026-09-02 (W0):** يبقى على روابط wa.me الحالية بلا Twilio/Cloud API — الأدمن يتحكم برقم الوجهة (إعدادات الفندق + حقل قابل للتحرير في معالج الوصول) · W1-W4 معلّقة حتى قرار مزود مستقبلي |
| بوابة دفع حقيقية | المرحلة P5 — مقيَّدة بقرار عمل (اختيار المزود في عدن/المنطقة) |
| Push notifications (FCM/APNs) | ضمن المرحلة F3 |
| بيئة إنتاج + نسخ احتياطي + مراقبة | المرحلة P |
| NestJS / PostgreSQL / Monorepo / OpenAPI codegen | **مؤجل بلا موعد — بقرار AD-07 — لا يُفتح إلا بمحفّز واقعي موثق (القسم 9.3)** |
| فصل الأوضاع الأربع إلى مسارات حقيقية | مسار موازٍ داخل F — بلا حق توقيف أي شيء |
| الإنجليزية الكاملة، تحليلات، OTA/Channel manager | المرحلة O4 — بعد التشغيل |

### 2.3 حدود الثقة الحالية (صراحة كاملة)

> **تحديث بوابة الثقة (2026-09-02):** الحد أدناه كان وصف نقطة الوقوف قبل المرحلة H. **الآن انتهى عهد «العين وحدها»**: 157 اختبارًا آليًا أخضر بأمر واحد (`bun run test`) يغطي التسعير والعربون والرصيد والأكواد والرحلة الذهبية كاملة الأرقام (H2) — وكل إصلاح مستقبلي في مسارات المال/الدومين يرافقه اختبار انحدار (AD-09).

*(الوصف التاريخي قبل H:)* كل التحقق حتى تلك اللحظة كان **بالعين البشرية عبر المتصفح، مرة، في جلسات**. لا اختبار آلي واحد. هذا كان مقبولًا لنموذج أولي مكتمل، وغير مقبول كأرضية لعميل Mobile مثبَّت على هواتف الناس — وهذا تحديدًا ما عالجته المرحلة H (الآن مقفلة).

---

## 3. سجل القرارات الحاكمة (ADR v2)

> هذه القرارات مستمدة من جلستي القرار مع المالك ومستندة إلى الواقع المنفَّذ. تغيير أي منها = قرار جديد موثَّق بموافقة المالك.

**AD-01 — الـ Backend الرسمي للنسخة الأولى هو مسارات API داخل Next.js.**
الواقع: **67 ملف مسار / 82 نقطة نهاية** تعمل فوق Prisma واحد (تصويب الجرد H4-a). بديل مرفوض: استخراج NestJS الآن — سنة إعادة بناء بلا قيمة للضيف، واحتمال واقعي ألا نعود لمستوى الاكتمال الحالي. أي استخراج مستقبلي = strangler route-by-route بمحفّز موثق (AD-07).

**AD-02 — SQLite يبقى للإنتاج أحادي الفندق.**
فندق واحد، 14 غرفة، عشرات المستخدمين المتزامنين، نسخة خدمة واحدة مع WAL ونسخ احتياطي ليلي — تشغيل مشروع ومدافَع عنه أمام أي مهندس. العيب الحقيقي لم يكن المحرك قط، بل غياب Migrations — وهذا ما تصلحه H1. PostgreSQL يُفتح بمحفّز (AD-07) لا بمزاج معماري.

**AD-03 — Flutter = نقل (Port) تصميم مُثبَت، لا استكشاف.**
تطبيق الضيف موجود فعلًا mobile-first مجرَّب تدفقاته فوق API كامل. Flutter ينقل شاشة بشاشة وسلوكًا بسلوك من الواجهة المُثبتة. هذا يجعل مرحلة F أرخص وأقل مخاطرة من 90% من مشاريع التطبيقات، ويحصر الاكتشاف في طبقة Flutter الخالصة (إدارة حالة، بناء، متاجر).

**AD-04 — بوابة عبور Flutter هي قائمة مغلقة من أربعة: Migrations، اختبارات المال، عقد التوكن، وثيقة العقد المستقر.**
ليس لأنها المهمة الوحيدة في العالم، بل لأنها الأشياء التي **يتضاعف ثمنها أو يتغير شكلها جذريًا** بعد وجود تطبيق في المتاجر: API يُجمَّد بالمتاجر، خطأ فوترة يُصلح عبر دورة إصدار، والتفاف Flutter حول الجلسات يتحول دَينًا تقنيًا بعمر سنوات.

**AD-05 — WhatsApp مسار أعمال مستقل، ليس شرطًا تقنيًا لـ Flutter.**
قيمته التشغيلية في سوق عدن مرتفعة (الضيف يعيش على واتساب)، لذلك يبدأ مبكرًا (W0 بمجرد انطلاق F) — لكنه لا يحمل أي بوابة تقنية ولا يوقف شيئًا إن تأخر اعتماده التجاري.

**AD-06 — لا إعادة فتح للمنطق المُثبَت.**
الدومين (Reservation/Stay/الأنواع/الأكواد/لقطة السعر/الفاتورة) مُجرَّب طرفًا لطرف. إعادة تصميمه الآن ليست تحصينًا؛ هي خطر متنكّر في هيئة تحصين. الأسلوب الوحيد المسموح به معه: **تغطية بالاختبارات، لا فتح وتعديل** (H2).

**AD-07 — محفزات الاستخراج المعماري (NestJS/PostgreSQL/Monorepo) موثقة ومؤجلة.**
لا يُفتح أي منها إلا بواقع: فندق ثانٍ حقيقي، أو فريق backend منفصل عن فريق الويب، أو حمل يفوق نسخة خدمة واحدة. وحين يُفتح: هجرة هادئة عبر Prisma و strangler — لا قفزة. التفصيل في القسم 9.3.

**AD-08 — اختيارنا الظرفي: الخيار A مع سقف وقائمة مغلقة.**
أخطر خطأين: تأخير Flutter بتحصين زائد (خطأ **مرئي** قابل للكبح)، أم بدء مبكر يكشف عيب فوترة/مصادقة بعد النشر (خطأ **مخفي** بطبيعته يظهر كحادثة في يد ضيف). قاعدة القرار: إذا كان أحد الخيارين قابلًا للرصد الذاتي والآخر لا، فاختر القابل للرصد — مع سقف زمني يمنع انزلاق A نحو إعادة بناء مستمرة.

**AD-09 — التحقق الآلي يحل محل العين من الآن فصاعدًا.**
من قفل H2 فصاعدًا: أي إصلاح لخلل في مسارات المال/الدومين يرافقه اختبار يُثبت الخلل ثم يُثبت إصلاحه (regression test) — وإلا يُعد الإصلاح غير مكتمل.

**AD-10 — فصل بيانات العرض عن بيانات التشغيل قبل أول ضيف حقيقي.**
حتى قفل P2 تُعد بيانات Seed الحالية (أكواد العرض التجريبية في الملحق ب) بياناتًا تجريبية تُمسح قبل الإطلاق. لا يُسمح بقيام دائم لأي كود تجريبي في بيئة الإنتاج.

---

## 4. خريطة المسار الكاملة (The Route)

```
[منصة مكتملة الوظائف، متحقق منها بالعين]
        │
        ▼
┌─ المرحلة H: التحصين ─ H1..H4 ─ ✅ مكتملة ومقفلة (2026-09-02) ─┐
        │
        ▼  ◄── بوابة الثقة (القسم 5) — ✅ مقفلة
┌─ المرحلة F: Flutter — نقل تصميم مُثبَت ─ F0..F8 ──────────────┐
│   (يبدأ W0 بالتوازي: الاعتماد التجاري لواتساب — ذيل طويل)     │
        │
        ▼  ◄── بوابة المتجر (القسم 6.10)
┌─ المرحلة W: WhatsApp — مسار الأعمال ─ W1..W4 ─────────────────┐
        │  (تسير بالتوازي مع ذيل F، لا تسلسلًا صارمًا بعده)
        ▼  ◄── بوابة الإطلاق (القسم 8.8)
┌─ المرحلة P: تجهيز الإنتاج والإطلاق ─ P1..P7 ──────────────────┐
        │
        ▼
┌─ المرحلة O: ما بعد الإنتاج — التشغيل والتوسع ─ O1..O4 ────────┐
        (مستمرة — إيقاعات تشغيل + حلقات تحسين + محفزات استخراج)
```

**اصطلاح الترقيم الجديد:** مراحل الخطة القديمة (P0–P13) **منجزة ومُبتلعة** في جرد القسم 2 — الترقيم الحي الجديد هو H / F / W / P / O. أي إشارة مستقبلية لمرحلة تعني هذا الترقيم.

**اصطلاح حساب الخطوة:** كل خطوة أدناه تحمل: **القبول** (شرط القفل بدلالة قابلة للفحص) · **يعتمد على** · **الجهد** (S = ساعات / M = أيام قليلة / L = أسبوع+) · **خطر الإيجاز** (ما الذي يحدث لو تأجلت). الخطوات داخل المرحلة نفسها مرتبة بترتيب التنفيذ المقترح.

---

## 5. المرحلة H — التحصين (القائمة المغلقة) — ✅ مكتملة ومقفلة 2026-09-02

**هدف المرحلة:** تحويل «النموذج الأولي المكتمل» إلى «أرضية يمكن إيقاف عميل Mobile عليها». أربعة بنود فقط. سقف أسبوع عمل — **أُنجزت في يوم واحد بتنفيذ متوازٍ (موجتان من الوكلاء الفرعيين + موجة تكامل).**

### H1 — خط أساس Migrations — ✅ مقفل

| # | الخطوة | التفصيل |
|---|---|---|
| H1.1 | توليد الـ migration التأسيسي | `prisma migrate diff --from-empty --to-schema-datamodel` → ملف migration أولي يمثل المخطط الحالي كما هو (**22 نموذجًا** — تصويب الجرد) بلا أي تغيير تصميمي |
| H1.2 | تعليم القاعدة الحالية | `migrate resolve --applied` على قاعدة التطوير — القاعدة الحالية تصبح «على الخط الأساس» |
| H1.3 | التحقق من المسار النظيف | استنساخ نظيف → `migrate deploy` → `seed` → التطبيق يعمل — **إثباتًا أن المسار قابل لإعادة الإنتاج من الصفر** |
| H1.4 | تحديث التدفق والسكربتات | إزالة `db:push --accept-data-loss` من التدفق اليومي (يبقى مسموحًا لقواعد scratch التجريبية فقط)؛ تحديث README والأوامر |

- **القبول:** استنساخ جديد يعمل بمسار migrations فقط؛ `migrate status` صفر انحراف؛ أي تعديل مخطط لاحق يجتاز مراجعة ملف migration.
- **يعتمد على:** — (أول بند).
- **الجهد:** S.
- **خطر الإيجاز:** أول تغيير مخطط بعد دخول بيانات حقيقية = القرار بين فقدان بيانات أو تجميد تطوير. هذا أخطر بند مؤجل في المشروع كله.

### H2 — اختبارات آلية لمسارات المال (أثقل بند وأهمها) — ✅ مقفل

| # | الخطوة | التفصيل |
|---|---|---|
| H2.1 | تفعيل حامل الاختبارات | `bun test` جاهز + مجلد `tests/` + قاعدة اختبار على قاعدة معزولة (لا تلمس قاعدة التطوير أبدًا) |
| H2.2 | اختبارات وحدة محرك التسعير | سعر أساسي × ليالي · ضريبة 15% بتقريب السنت · زيادة نهاية الأسبوع · عربون 50% · عدد الليالي من حدود الأيام · **ال snapshot يتجمد ولا يتأثر بتغير أسعار لاحق** |
| H2.3 | اختبارات التوفر | فلترة السعة · تواريخ معكوسة مرفوضة · أفق الحجز · حجز فوق المخزون مرفوض · ذرية الإنشاء (لا oversell حتى مع طلبين متزامنين) |
| H2.4 | اختبارات رصيد الفاتورة | `grand + Σcharges − Σpayments` · حالة خروج برصيد موجب/صفر · رسوم التمديد/تغيير الغرفة تنضاف صحيحة |
| H2.5 | اختبارات الأكواد | صيغ H/R/A وتفرّدها · الهاش أحادي الاتجاه · منتهٍ/ملغي مرفوض · **كود الضيف يموت عند الخروج** (I11) |
| H2.6 | الاختبار الذهبي التكاملي | الرحلة الكاملة على قاعدة اختبار معزولة: حجز → وصول → تعيين غرفة + كود → طلب خدمة → رسوم → دفع → خروج → إغلاق — بتأكيدات على كل قيمة مالية في كل محطة |
| H2.7 | تثبيت العادة | أمر واحد أخضر = شرط قبل أي دمج — **نُفّذ كسكربت `bun run test` بسلسلة عمليات معزولة** (unit → auth → integration): bun يشغّل ملفات الاختبار في عملية واحدة وسنغلتون Prisma يتصارع بين مجموعات قواعد مختلفة، فالعزل بالعمليات هو التصميم الصحيح |

- **القبول:** `bun test` أخضر بأمر واحد؛ كل الدوال المالية الحتمية مغطاة بمساراتها الحدية؛ الاختبار الذهبي يعيد نتيجة مطابقة رقمًا رقمًا.
- **يعتمد على:** H1 (الاختبارات تعمل فوق migrations لا فوق push).
- **الجهد:** M.
- **خطر الإيجاز:** خطأ فوترة واحد بعد المتاجر يُصلَح مرتين (خادم + إصدار تطبيق عبر مراجعة متجر) بينما الضيف يرى الرقم الخاطئ بيده. الميزة الحالية — دوال حتمية معزولة كُتبت دون قصد بشكل قابل للاختبار — تتبدد مع أول تعديل يدوي.

### H3 — عقد المصادقة للعميل الثاني (Mobile-ready Auth) — ✅ مقفل (بتوفيق الواقع: بلا أي تغيير مخطط)

> **نتيجة التنفيذ (توثيق):** جرد H4-a كشف أن الكود **موحّد على Bearer أصلًا** (validate يعيد التوكن في الجسم، الويب يرسله `Authorization: Bearer` — لا كوكي قط). عقد موحد للويب والجوال موجود منذ البداية، والنموذج (Session: token/expiresAt/revoked/lastSeenAt) مكتمل. الفجوة الوحيدة كانت **التجديد**: سدّها `POST /api/auth/renew` (نفس التوكن يُمدّد إلى min(12س، صلاحية الكود) + rate-limit + تدقيق AUTH_RENEW + لمسة lastSeenAt كل 60 ثانية في getAuth) — **بلا أي migration** و«لكوكز» المذكورة في H3.2 أدناه لم تلزم أصلًا (الواقع أفضل من الخطة).

| # | الخطوة | التفصيل |
|---|---|---|
| H3.1 | توسيع الجلسة إلى توكن bearer | توكن عشوائي طويل + **عمر وانتهاء وتجديد** (أول استخدام حقيقي لمسار H1: التغيير عبر migration لا عبر push) |
| H3.2 | توحيد الدخول | `/api/auth/validate` يصدر التوكن؛ **الويب يلفّ نفس التوكن في cookie httpOnly** — توافق كامل بلا كسر سلوك الويب الحالي |
| H3.3 | الحماية والتدقيق | rate-limit على الإصدار/التجديد · إبطال التوكن مع إبطال الكود ومع الخروج · كل شيء في سجل التدقيق |
| H3.4 | اختبارات العقد | إصدار → استدعاء محمي → انتهاء → تجديد → إبطال — + سيناريو «عميل محمول كامل» بسكربت (دخول بالكود ثم استخدام التوكن) |

- **القبول:** سلوك الويب لم يتغير (تحقق بالمتصفح)؛ سيناريو الموبايل يعمل من سكربت؛ التوكن يموت عند إبطال الكود.
- **يعتمد على:** H1 · H2 (اختبارات الجلسة تُكتب على مسار الاختبار).
- **الجهد:** M.
- **خطر الإيجاز:** Flutter سيبني التفافًا حول الجلسات الحالية؛ الالتفاف يتحول دَينًا تقنيًا بعمر سنوات. هذا البند الوحيد الذي **يتغير شكله جذريًا** إن تأخر.

### H4 — وثيقة العقد المستقر (Stable Contract) — ✅ مقفل

| # | الخطوة | التفصيل |
|---|---|---|
| H4.1 | جرد مسارات Flutter | `docs/CONTRACTS.md`: كل نقطة نهاية سيلتهمها Flutter (وضع الضيف أولًا) بشكل طلب/استجابة ودلالات الأخطاء |
| H4.2 | قاعدة الاستقرار | تمييز STABLE · قاعدة الإصدار: أي كسر لاحق = قرار موثَّق + ترقية نسخة، لا تعديلًا صامتًا |
| H4.3 | الربط | مرجع إلزامي لمرحلة F — F0 يبني عميل API منه |

- **القبول:** الوثيقة تغطي كل ما يحتاجه وضع الضيف بالكامل، ومراجعة بصرية تؤكد مطابقتها للمُنفَّذ.
- **يعتمد على:** H3 (شكل المصادقة يثبت في العقد).
- **الجهد:** S/M.
- **خطر الإيجاز:** «الاستقرار» يظل إحساسًا لا التزامًا — أول refactor ويب بريء يكسر التطبيق بعد نشره دون أن يشعر أحد.

### 5.5 بوابة الثقة — ✅ **مقفلة 2026-09-02** (الأدلة أدناه)

عند تحقق الأربعة أدناه أُعلن بثقة: **الأرضية مستقرة بما يكفي لنبدأ Flutter Native**:

1. **Migration أساس واحد يعمل** بدل `db:push` — ✅ baseline مُطبَّق + مسار نظيف مجرّب (deploy + seed + عدّ سليم).
2. **أمر اختبار واحد أخضر** يغطي التسعير والعربون والرصيد والإلغاء والأكواد — ✅ **157 اختبارًا** بـ`bun run test` (124 وحدة + 7 مصادقة + 26 تكامل — الرحلة الذهبية بأرقام السنت في كل محطة).
3. **توكن بعمر وتجديد** صالح للجوال — ✅ Bearer موحّد + renew + اختبارات السيناريوهات السبعة + تحقق حي + سلوك الويب لم يتغير (متزرع بالمتصفح: دخول الضيف/الاستقبال/الإدارة كلها تعمل).
4. **العقد المستقر معلَن** — ✅ CONTRACTS.md **v1.0**: 82 نقطة نهاية موثقة من الكود + مجموعة F1 وعقد مصادقة الجوال معلَنان **STABLE** بقاعدة الإصدار (§9.2).

**عندها حرفيًا:** الـ API لا يتحرك عشوائيًا (له عقد)، الدومين ثابت (مغطى)، المال آمن بما يكفي (مُختبر)، ما بُني لا يُعاد بناؤه بسبب Flutter (Flutter يستهلك عقودًا معلَنة لا أشكالًا يكتشفها). **ولا شيء بعدها ينتظر — كل تحسين آخر مكانه F فما بعد.**

---

## 6. المرحلة F — Flutter (نقل تصميم مُثبَت)

**هدف المرحلة:** تطبيق Android + iOS حقيقي بالأوضاع الثلاثة، منقولًا من الواجهات المُجرَّبة — لا مصممًا من فراغ. **القاعدة الحاكمة: كل شاشة Flutter لها مرجع سلوكي واحد هو الواجهة الويب المناظرة + عقد API في CONTRACTS.md.**

> ملاحظة تنفيذية: مشروع Flutter يُنشأ في المستودع (مجلد `mobile/`) بذات صفحة GitHub — منفصل تمامًا عن تطبيق الويب، يشترك معه فقط في العقود والهوية البصرية.

### 6.1 F0 — التأسيس ✅ (منفَّذة 2026-09-02)
مشروع Flutter (معرّف ناشر، بيئتا dev/prod flavors)، قرار إدارة الحالة (**ChangeNotifier خالص — بلا حزم خارجية**)، عميل API بالتوكن مع اعتراض 401→تجديد تلقائي (سياسة §1.2.1)، RTL + خط Cairo، ثيم الألوان (كحلي/ذهبي) بمطابقة الويب.
**المنفَّذ فعليًا:** `mobile/` كامل الملكية (38 ملف dart) + `com.cairoheart.aden` + flavors dev/prod + Cairo مدمج كأصول + CI/Release على GitHub Actions بقرار المالك (لا Flutter SDK محليًا) — تحقق مزدوج: CI + تحقق المتصفح للويب بعد كل تغيير مشترك.
**القبول:** ✅ بناء Android أخضر على CI (Gradle + flavors + Kotlin) + دخول بالكود عبر التوكن يعمل بنفس عقد الويب.

### 6.2 F1 — وضع الضيف (الأول دائمًا — أعلى قيمة، تدفقاته المُجرَّبة كاملة) ✅ (منفَّذة 2026-09-02)
نقل شاشة بشاشة: دخول بالكود → لوحة الإقامة → الطلبات بدورة حياتها → الفاتورة → المحادثة → الإشعارات → التمديد/تغيير الغرفة/طلب الخروج/الملاحظات.
**المنفَّذ فعليًا:** كل الشاشات منقولة من مراجع الويب حرفيًا (وكلاء 13-a1/13-a2/13-b — انحرافاتهم الموثقة في worklog) + شاشة إعداد الخادم عند غياب العنوان المخبوز + شاشة إرشادية لأكواد الطاقم (F4/F5 لاحقًا).
**القبول:** ✅ التدفقات فوق عقد CONTRACTS.md فقط — صفر نداء HTTP خارج العقد (النداءات كلها في GuestStore/ApiClient واختباراتها خضراء على CI — 33 اختبار Flutter + 157 ويب).

### 6.3 F2 — الوقت الفوري داخل التطبيق ✅ (منفَّذة 2026-09-02)
عميل Socket.IO في التطبيق — **المقدمة أولًا** (التطبيق مفتوح = يعيش لحظيًا كالويب تمامًا). طوبولوجيا الخدمة الحالية تُستخدم كما هي.
**المنفَّذ فعليًا:** RealtimeService (path=/ + XTransformPort=3002 + join stay:{id} + الأحداث بنفس معالجات الويب في GuestShell) + استطلاع المحادثة كل 12 ثانية كالويب حرفيًا (guest-context.tsx:256). التحقق الحي على جهاز حقيقي ضمن F7.
**القبول:** مُنفَّذ على العميل — التحقق الحي (رسالة استقبال تظهر لحظيًا والتطبيق بالمقدمة) ضمن تجربة F7 Dogfooding.

### 6.4 F3 — الإشعارات الفورية (Push)
FCM (Android) / APNs (iOS) — هنا تكتمل قصة الخلفية: حدث يصل والضيف مغلق التطبيق. سير العمل: الخادم يصدر إشعارًا مركزيًا (النموذج موجود) → جسر يوجّهه إلى FCM/APNs مع التوكن الجهازي.
**القبول:** إشعار طلب خدمة/رسالة يصل والهاتف مقفلة، والنقر يفتح الشاشة الصحيحة.
**الجهد:** M/L · **يعتمد على:** F1 (وأول استخدام لقرار FCM خارج بيئة الاختبار).

### 6.5 F4 — وضع الاستقبال (موجَّه للأجهزة اللوحية) — ✅ منفَّذة 2026-09-03
نقل لوحة الاستقبال: وصولات/مغادرات/مقيمون، معالج الوصول 4 خطوات (تعيين الغرفة + كود الضيف مرة واحدة)، الخروج بفحص الرصيد، الطلبات، لوحة الغرف، البحث.
**المنفَّذ فعليًا (F4-a · Task 19):** توجيه دور RECEPTION إلى `ReceptionShell` (بدل الplaceholder) + `ReceptionStore`/نماذج فوق عقود R-01..R-07/R-10/R-22/R-23 حرفيًا (صفر تغيير خادم — القناة كاملة كانت جاهزة) + غرفة `reception` في Realtime بنفس أحداث الويب + لوحة التحكم + الوصولون + **معالج الوصول 4 خطوات** (كود الضيف مرة واحدة + wa.me برقم وجهة قابل للتعديل وفق W0 — أُضيفت `url_launcher`) + المغادرون + **معالج الخروج 3 خطوات** (تسوية بدفعة سريعة R-12 أو تأكيد مع رصيد + تنفيذ R-07) + إشعارات الاستقبال (تعليم تلقائي عند الفتح).
**المنفَّذ فعليًا (F4-b · Task 20):** القشرة 6 تبويبات (NavigationRail للواسع/تنقل سفلي للضيق — كالويب) + زر بحث عام في الهيدر · **المقيمون** (R-04) ببطاقات (الرصيد/الطلبات النشطة/موعد الخروج بأحمر إذا حلّ) · **تفصيل الإقامة** (R-05) صفحة كاملة بخمسة تبويبات: الضيف (لقطة سعر الليالي) + الفاتورة (R-12 دفعة/R-13 بند بحواريهما الحرفيينَ) + الطلبات (→ تفصيل) + **الرسائل (R-20/R-21 محادثة فورية)** + الإجراءات (**قرارات التمديد R-15/R-16 وتغيير الغرفة R-17/R-18** + محادثة + خروج) · **الطلبات** (R-08) بفلاترها الثمانية + العاجل فقط + **تفصيل الطلب** (R-09 بخط زمني وخريطة إجراءات الحالة الحرفية — استلام/إسناد بفريق/تنفيذ/انتظار/إكمال/رفض/إلغاء بتأكيد) · **لوحة الغرف** (R-10) بطوابق وملونة + **انتقالات الغرفة** (R-11: تنظيف/متاحة/خارج الخدمة/إعادة — بتأكيدين) · **البحث** (R-19 debounce 350ms → حجز/إقامة) · وصل كل نقاط F4-b المعلّقة في اللوحة (KPI المقيمين/الطلبات قابلتان للنقر + صف الطلب يفتح تفصيله + زر الفاتورة في المغادرين + إجراءات سريعة: الغرف/البحث). 13+ اختبار شاشة جديدًا.
**القبول:** ✅ استقبال يجري وصولًا وخروجًا كاملين من جهاز لوحي — الآن مع إدارة كاملة للمقيمين والطلبات والغرف والبحث.
**الجهد:** L (منفَّذ) · **يعتمد على:** F0 (كان يُنفَّذ بالتوازي مع F2/F3 فنيًا).

### 6.6 F5 — وضع الإدارة ✅ (منفَّذة 2026-09-04)
نقل أقسام الإدارة كاملة.
**المنفَّذ فعليًا (Task 21):** `AdminStore` فوق A-01..A-34 حرفيًا (صفر تغيير خادم — كل القناة متعاقدة ومستهلكة من الويب) + `ApiClient.delete()` (تلزمه A-07/A-11/A-14/A-18/A-22) + غرفة `admin` في Realtime بنفس معالجات الويب + `app.dart` يوجّه ADMIN→AdminShell + حذف RolePlaceholder (كل الأدوار حقيقية) · **11 قسمًا كاملة:** لوحة تحكم (4 KPIs حرفية + توزيع الغرف بألوان الويب + إيراد 14 يومًا + أحدث الحجوزات + تنبيهات + أكواد نشطة→توليد) · إعدادات الفندق (كل الحقول + minAppVersion من F6) · أنواع الغرف (النموذج الكامل) · الغرف (إنشاء/تعديل بلا OCCUPIED يدويًا/حذف) · المعدلات (تحذير التداخل يُعرض والإنشاء ينجح) · الخدمات وأقسامها · **الطاقم والأكواد (توليد كود بحوار مطابقة الدور + الكود الخام مرة واحدة بحدود ذهبية ونسخ + إبطال بتأكيد يذكر الجلسات)** · الحجوزات (مصفّحة + تفصيل بلقطة السعر الحرفية) · الضيوف · التقارير · سجل التدقيق (24 فعلًا مُصفّحًا) · القشرة: شريط جانبي قابل للطي للواسع + سفلي بـ4 أساسية+«المزيد» (نفس MOBILE_PRIMARY في الويب) + جرس A-34.
**القبول:** ✅ العمليات اليومية للإدارة (توليد/إبطال كود، إدارة غرفة، مراجعة تدقيق) تعمل من التطبيق — عبر وكلاء موجهين بدقة (21-a/21-b/21-c) + 68 اختبارًا جديدًا (169 خضراء في CI) + تحقق حي بالقناة نفسها (توليد/إبطال من الويب).
**الجهد:** M (منفَّذ) · **يعتمد على:** F4 (أنماط مشتركة).

### 6.7 F6 — خط البناء والإصدار ✅ (مكتملة نهائيًا 2026-09-02)
بناء APK لـ Android · ترقيم إصدارات · فرض حد أدنى لإصدار التطبيق بدفع من الخادم (استجابة تحمل `minAppVersion`).
**المنفَّذ فعليًا:** mobile-ci (analyze + test + build smoke) وmobile-release (APK موقّع بمفتاح في أسرار GitHub → GitHub Release بملفّي prod/dev) — التوقيع والإصدار من أمر واحد ✓ · **minAppVersion منفَّذ (Task 18):** PUB-07 `GET /api/public/app-config` (عام، fail-open) + حقل الأدمن في A-03/إعدادات الفندق (x.y.z أو فارغ) + حارس التطبيق عند الإطلاق (شاشة حجب «يتوفر تحديث مطلوب» برابط قابل للنسخ) + `--dart-define=APP_VERSION` من pubspec في CI — عقد CONTRACTS v1.1 (امتداد موثَّق) · iOS/TestFlight مؤجل لما بعد بوابة المتجر.
**القبول:** ✅ التحقق المحلي: 165 اختبارًا أخضر (منها 8 جديدة) + lint نظيف + دورة E2E بالمتصفح (تعيين/إفراغ الحقل → انعكاس فوري في PUB-07) · تحقق CI النهائي على GitHub (web-ci + mobile-ci) بتاريخ الدفع.

### 6.8 F7 — التجربة الداخلية (Dogfooding) — 🟡 العتاد مكتمل + Day-0 منفَّذ (2026-09-05) · التقويم الحي بيد المالك
تشغيل حقيقي محدود: طاقم الفندق + ضيوف مختارون لأسبوعين. قائمة إصلاح مُدارة بصرامة (خلل → إصلاح + اختبار انحدار وفق AD-09).
**المنفَّذ فعليًا (Task 24 · 2026-09-05):** docs/DOGFOOD_PLAN.md (بروتوكول 14 يومًا + تصنيف SEV-1/2/3 + قواعد AD-09/الإيقاع/معايير الخروج) · docs/DOGFOOD_FINDINGS.md (قائمة الإصلاح الحية) · `scripts/purge-demo.ts` (AD-10 — جرد صفر تام + كود إقلاع أدمن خام مرة واحدة) · `scripts/smoke.ts` (فحص يومي آلي للمسار الذهبي — 25 فحصًا حيًا يغطي I6/I8/I11/§12.2) · **Day-0 منفَّذ فعليًا**: مسح 106 صفوف تجريبية → smoke بالكود الحقيقي 25/25 أخضر (مراجع HTL/ST/REQ عادت من الصفر صح) → النسخ الاحتياطي والتدريب (راجع P2) — التسلسل الحي على أرض الفندق (14 يومًا بلا SEV-1) مسؤولية المالك بمواعيقه البشرية.
**القبول:** أسبوعان بلا حادثة مالية أو مصادقة واحدة؛ قائمة الخلل الوظيفي مغلقة أو مؤجلة بقرار. *(العتاد جاهز · التقويم لم يبدأ بعد)*
**الجهد:** M (جهد) / زمن تقويمي أسبوعان+ (لا يُختصر).
**يعتمد على:** F1..F3 للضيوف · F4 للاستقبال.

### 6.9 F8 — تقديم المتاجر — 🟡 الحزمة مكتملة (2026-09-05) · التقديم بعد بوابة §6.10 (شهادة F7)
إرسال Google Play + App Store، سياسات الخصوصية، لقطات، بيانات الحساب التجاري.
**المنفَّذ فعليًا (Task 24):** docs/PRIVACY_POLICY.md v1.0 (SSOT) + قسم الخصوصية بالموقع (حوار في التذييل وبطاقة السياسات) + شاشة الخصوصية بالتطبيق (زر الدرع + الإصدار + 4 اختبارات) + docs/STORE_SUBMISSION.md (نصوص Play العربية + إجابات Data Safety + قائمة اللقطات + خطوات التقديم + قرار AAB + ردود الرفضات المتوقعة) — بوابات بوابة §6.10 موثقة الحالة داخلها. التقديم نفسه بيد المالك بعد شهادة أسبوعي F7.
**الجهد:** M (الجهد) — ذيل زمني مراجعات خارج تقديرنا.
**يعتمد على:** بوابة المتجر.

### 6.10 بوابة المتجر (شرط F8)
F1–F4 مقفولة بقبولها · F7 ناجحة بمعاييرها · عقد CONTRACTS.md لم يُكسر منذ قفل H4 (كل تغيير جرى عبر قرار موثَّق) · خطة الحد الأدنى للإصدار (F6) تعمل ✓ (منفَّذة — Task 18).

**مسار موازٍ داخل F (بلا حق توقيف أي خطوة): فك SPA إلى مسارات حقيقية.** الموقع يتفكك إلى `/` عام + مسارات دخول/أوضاع تدريجيًا حسبما يسمح به التطور — تحسين صيانة ويب صرف، لا يعيق Flutter ولا يعطَّل بسببه.

---

## 7. المرحلة W — WhatsApp (مسار الأعمال)

**المبدأ:** واتساب في سوق عدن قناة وصول الضيف اليومية قبل التطبيق. التطبيق للتحكم أثناء الإقامة؛ واتساب للوصول والوعد والحضور.

> **قرار المالك (W0) — 2026-09-02:** **لا ربط حاليًا مع واتساب أو Twilio** — القناة تبقى كما هي: روابط `wa.me` يفتحها المستخدم في واتساب نفسه. **الأدمن هو المتحكم برقم الواتساب الذي يتم الإرسال إليه:** رقم الفندق من إعدادات الفندق (A-03: `hotel.whatsapp`) يغذي كل روابط الموقع، **ورقم وجهة كود الضيف في معالج تسجيل الوصول صار حقلًا قابلًا للتحرير** (افتراضيًا هاتف الضيف من الحجز — يعدّله الاستقبال/الإدارة قبل الإرسال). W1..W4 أدناه **معلَّقة بلا موعد** حتى قرار مزود مستقبلي (Cloud API/وسيط) — عند فتحها تُستأنف كما هي مكتوبة.

| # | الخطوة | التفصيل |
|---|---|---|
| W0 | ~~قرار المزود والاعتماد~~ | ✅ **مقفل بقرار المالك 2026-09-02: بلا مزود — wa.me + تحكم الأدمن برقم الوجهة** (أعلاه) |
| W1 | الإشعارات الصادرة | تأكيد الحجز (المرجع والتفاصيل) · **تسليم كود الضيف عند Check-In** · تحديثات الطلبات · تذكير الخروج — عبر توسيع نظام الإشعارات المركزي القائم (قناة WHATSAPP بجوار IN_APP) — **معلَّقة حتى قرار مزود** |
| W2 | القوالب والامتثال | قوالب معتمدة، سياسة الموافقة، ساعات الهدوء — **معلَّقة** |
| W3 | الوارد | ردود الضيف تُربط بالحجز/الإقامة وتُسلَّم للاستقبال (handoff بشري أولًا — لا روبوت ردود في النسخة الأولى) — **معلَّقة** |
| W4 | التدقيق | كل رسالة صادرة/واردة في سجل التدقيق مرتبطة بكيانها — **معلَّقة** |

- **القبول (خام W):** ضيف يحجز → رسالة تأكيد تصل واتساب · ضيف يصل → كوده يصل واتساب · يرد → يظهر للاستقبال.
- **الجهد:** W0 S (جهد) لكن تقويميًا أسابيع اعتماد · W1 M · W2 S · W3 M · W4 S.
- **خطر الإيجاز:** لا خطر تقنيًا على Flutter (مستقل تمامًا) — الخطر تجاري: تفويت قناة يتنفس منها العميل الأول.

---

## 8. المرحلة P — تجهيز الإنتاج والإطلاق — 🟢 الحزمة مكتملة (Task 24 · 2026-09-05) · التنفيذ على الخادم بيد المالك

**حالة ما بعد Task 24:** P1 عتاد deploy/ كامل (Caddy إنتاجي + systemd + سكربت نشر بتراجع) · **P2 مقفولة فعليًا: تدريب استعادة حقيقي منفَّذ وموثّق في RESTORE_DRILL §4 (حذف القاعدة → استعادة → أعداد مطابقة → خدمة عادت — ≈ دقيقتين ≪ ساعة البوابة)** + scripts/backup-restore فوق bun:sqlite (VACUUM INTO + quick_check) + مسح AD-10 · P3 حية (/api/health + health-check.sh + تنبيه WEBHOOK) · P4 RUNBOOK بـ6 بطاقات · P5 وثيقة قرار مفتوحة عمدًا · P6 قائمة أمن + **DEP-01 مُصلح فعليًا: next 16.1.3→16.3.4 (165 اختبارًا أخضر)** · P7 LAUNCH_PLAN. المتبقي: تشغيل VPS من deploy/README + DOMAIN وقناة التنبيه وقرارات المالك (راجع LAUNCH_PLAN §قائمة الإقلاع).

**هدف المرحلة:** من «يعمل على خادم التطوير» إلى «يعمل لفندق حقيقي بضيوف حقيقيين ويمكن إصلاحه وهو نائم».

| # | الخطوة | التفصيل | القبول |
|---|---|---|---|
| P1 | بيئة الإنتاج | خادم (VPS) · نطاق + TLS · عكس وكيل · مدير عمليات للخدمات الثلاث (تطبيق + realtime + البث) · **إدارة أسرار** (بلا أي سر في المستودع) | التطبيق يعمل من النطاق عبر HTTPS بإعادة تشغيل تلقائية بعد الفشل |
| P2 | انضباط البيانات | مسح بيانات العرض التجريبية (AD-10) · بيانات تشغيل حقيقية (فندق/غرف/طاقم/أكواد R/A حقيقية) · **نسخ احتياطي ليلي**: نسخ SQLite الآمن + نقطة تحقق WAL + نسخة خارج الموقع + **تدريب استعادة فعلي** · سياسة احتفاظ | استعادة نسخة أمس على خادم نظيف خلال < ساعة، مُجرَّبة يدويًا مرة على الأقل وموثقة |
| P3 | المراقبة | سجلات مهيكلة · تنبيه أخطاء (بريد/واتساب للمشغل) · فحص جاهزية دوري · عادة مراجعة التدقيق | خطأ حرج يصل إشعارًا خلال دقائق؛ لوحة/سجل جاهزية واضح |
| P4 | دليل التشغيل Runbook | الحوادث: توقف، استعادة قاعدة، **تسريب كود** (الإبطال معروف ومُجرَّب) · روتين الطاقم: إصدار/إبطال أكواد عند دوران الموظفين · قائمة يومية للاستقبال | أي مشغل جديد يستطيع تشغيل الروتين من الدليل وحده |
| P5 | بوابة الدفع الحقيقية | **مقيَّدة بقرار العمل** (أي مزود يعمل في عدن/المنطقة) · تفعيل مسار CARD كدفع حقيقي مع تحقق رد المزود (I2: لا دفع ناجح كاذب إلا بمصدر موثوق) · Idempotency كما هو | عربون حقيقي يُحصَّل ويظهر في الفاتورة والمدفوعات بلا إدخال يدوي |
| P6 | تدقيق أمني نهائي | فرض HTTPS · ضبط rate-limits لمقياس الإنتاج · تدوير التوكنات/الأسرار · فحص الاعتماديات | قائمة تحقق مغلقة بلا بنود مفتوحة حرجة |
| P7 | الإطلاق المرحلي | قائمة إطلاق + خطة تراجع (rollback) · **Soft launch**: الويب أولًا أسبوعين سليمين → فتح التطبيق → فتح واتساب | أسبوعا soft launch صفران من حوادث المال/المصادقة |

**8.8 بوابة الإطلاق:** P1–P4 مقفولة **+ تدريب الاستعادة مُجرَّى فعليًا (P2) — هذا البند تحديدًا لا يُقبل بالكلام** + P6 مغلقة. P5 قد يظل مفتوحًا إذا كان قرار المزود لم يحسم بعد (الدفع عند الفندق صياغة تشغيلية صادقة ومكتملة بذاتها).

---

## 9. المرحلة O — ما بعد الإنتاج (التشغيل والتوسع)

هذه المرحلة **مستمرة بلا نهاية** — والفرق بين مشروع يعيش سنوات ومشروع يتحلل بعد سنة هو انتظامها، لا ذكاؤها.

### 9.1 O1 — إيقاعات التشغيل الثابتة
| الإيقاع | المحتوى |
|---|---|
| يومي (آلي) | النسخ الاحتياطي الليلي نجح + فحص الجاهزية |
| أسبوعي | مراجعة أخطاء السجلات + عينات من سجل التدقيق + بنود قائمة الإصلاح |
| شهري | تحديث اعتماديات محسوب + **تدريب استعادة نسخة احتياطية** + مراجعة أكواد غير مستخدمة/منتهية |
| ربع سنوي | مراجعة أمنية + مراجعة محفزات AD-07 (هل ظهر فندق ثانٍ؟ فريق منفصل؟ حمل؟) |

### 9.2 O2 — حلقة تحسين المنتج
ملاحظات الضيوف والطاقم → قائمة صغيرة مرتبة بالقيمة → إصدارات صغيرة متكررة (لا إصدارات ضخمة) — وكل إصلاح في مسار مال/دومين بمرافقة اختبار انحدار (AD-09) وكل تغيير API عبر قرار العقد المستقر (H4).

### 9.3 O3 — محفزات الاستخراج المعماري (متى — لا هل)
| المحفّز الواقعي | يفتح |
|---|---|
| فندق ثانٍ حقيقي (عقد موقَّع) | قرار multi-tenant + **PostgreSQL عبر Prisma** + نموذج Tenant — هجرة هادئة مجرَّبة على نسخة أولًا |
| فريق backend منفصل عن فريق الويب | استخراج NestJS **route-by-route (strangler)** — لا قفزة |
| حمل يفوق نسخة خدمة واحدة / حاجة تعدد نسخ | فصل الخدمات + قاعدة خارجية + موازن حمل |
| **لا شيء مما سبح** | **لا شيء يُفتح — مهما بدت المعمارية أجمل على الورق** |

### 9.4 O4 — مسارات النمو اللاحقة (مرتبة بالقيمة التقديرية)
تعميق واتساب (مساعد ردود) · بوابات دفع إضافية · الإنجليزية الكاملة · تحليلات/لوحة إيراد للإدارة · PWA للويب · قنوات OTA/Channel manager — **لا يُفتح منها شيء قبل أن يطلبه واقع التشغيل.**

---

## 10. القوائم المجمدة — ما لا نلمسه الآن ولماذا

| المجال | القرار | السبب |
|---|---|---|
| نموذج الدومين + مخطط Prisma | يُجمَّد ويُغطى بالاختبار (H2) — لا يُعاد تصميمه | مُثبَت طرفًا لطرف؛ إعادة فتح المنطق المجرب خطر متنكر كتحصين (AD-06) |
| محرك التسعير من الداخل | يُختبر لا يُفتح | حتمي ومعزول — أفضل ما فيه أنه كما هو |
| طوبولوجيا الـ Realtime | تبقى كما هي حتى F3/P | تعمل ومُتحققة؛ الحاجة الحقيقية تظهر مع الخلفية/Push |
| NestJS / PostgreSQL / Monorepo / OpenAPI codegen | مؤجل بمحفز موثق (9.3) | إعادة بناء بلا قيمة للضيف (AD-01/02/07) |
| فك الـ SPA إلى مسارات | مسار موازٍ داخل F — بلا حق توقيف | تحسين صيانة ويب صرف؛ Flutter لا يهتم به |
| استبدال نظام الأكواد/الجلسات بـ NextAuth | مرفوض | يكسر المُجرَّب (rate-limit/هاش/إبطال/تدقيق) مقابل جمالية على الورق؛ الإضافة المطلوبة (توكن H3) صغيرة فوق بنية سليمة |
| أي «بينما نحن هنا…» | محظور (MUST NOT) | الطريق المعروف من تحصين قصير إلى إعادة بناء مفتوحة |

---

## 11. سجل المخاطر (أعلى المخاطر وأكثرها واقعية)

| # | الخطر | الاحتمال | الأثر | التخفيف المدمج في الخطة |
|---|---|---|---|---|
| R1 | انزلاق المرحلة H من قائمة مغلقة إلى إعادة بناء مستمرة | متوسط | مرتفع (يؤخر كل شيء) | السقف الأسبوعي (1.3) + القائمة المغلقة + بند خامس = «بعد Flutter» افتراضيًا |
| R2 | خطأ فوترة يظهر بعد المتاجر | منخفض قبل H2 / مرتفع بدونه | حرج (ثقة لا تُشترى) | H2 + AD-09 (اختبار انحدار مع كل إصلاح) + بوابة المتجر |
| R3 | ذيل اعتماد WhatsApp التجاري يطول | متوسط | متوسط (قناة وصول مؤجلة) | W0 يبدأ مع انطلاق F — لا يوقف التطبيق |
| R4 | كسر عقد API صامتًا يكسر التطبيق المثبَّت | متوسط | مرتفع | H4 (قاعدة الإصدار) + F6 (فرض حد أدنى للإصدار من الخادم) |
| R5 | فقدان بيانات تشغيل (لا نسخ/لا استعادة مجربة) | منخفض بعد P2 / مؤكد بدونه | كارثي | P2 + التدريب الشهري (O1) — الاستعادة **يجرب** لا يُفترض |
| R6 | خادم واحد يتعطل دون إشعار | متوسط | مرتفع | P1 (إعادة تشغيل تلقائية) + P3 (تنبيه) |
| R7 | دوران موظفين يترك أكواد R/A حية | متوسط | أمني | P4 (روتين الإبطال) + I4 (فحص الحالة مع كل طلب أصلًا) |

---

## 12. الملحق المرجعي — الثوابت المنفَّذة (منقولة من v1.0 الصالح منها)

### 12.1 الثوابت التجارية المفروضة خادميًا (Invariants)

| # | الثابت | مكان الفرض |
|---|---|---|
| I1 | لا حجز مؤكد فوق المخزون | عدّ التوفر داخل معاملة عند الإنشاء |
| I2 | لا دفع ناجح كاذب | الخادم يقرر؛ الواجهة لا تدّعي (البوابة الحقيقية في P5) |
| I3 | ضيف لا يرى بيانات ضيف آخر | RBAC + فلترة stayId في كل مسار guest |
| I4 | كود منتهٍ/ملغي لا يعمل | فحص status + expiry مع كل طلب |
| I5 | مرجع حجز واحد لحجز واحد | unique على bookingReference |
| I6 | لا يُعتمد سعر من العميل | computeQuote خادميًا عند الإنشاء |
| I7 | لا حجز مزدوج | معاملة ذرية + إعادة فحص داخلها |
| I8 | لا دفع مكرر | Idempotency Key |
| I9 | الحجوزات التاريخية تُفسَّر بلقطة وقت الحجز | Price Snapshot غير قابل لإعادة الكتابة |
| I10 | كل عملية حرجة مُدقَّقة | audit() مع كل معاملة حرجة |
| I11 | كود الضيف يموت عند الخروج | check-out يُبطل الكود والجلسات |
| I12 | الغرفة المشغولة لا تُغيَّر يدويًا | رفض OCCUPIED من الإدارة |

### 12.2 قواعد المال
سنت صحيح دائمًا · الضريبة = `round(subtotal × taxPercent / 100)` · رصيد الإقامة = `grand + Σ charges − Σ paid` · التفسير التاريخي عبر snapshot حصرًا.

### 12.3 آلات الحالة (المصدر: الكود المنفَّذ)
`Reservation`: PENDING → CONFIRMED → CHECKED_IN → COMPLETED · إلغاء من CONFIRMED/PENDING وفق سياسة 24 ساعة · NO_SHOW لمن لم يحضر.
`Stay`: يُنشأ عند Check-In → ACTIVE → (CHECKOUT_REQUESTED) → CLOSED.
`ServiceRequest`: **NEW** → ACKNOWLEDGED → IN_PROGRESS → COMPLETED/CANCELLED (تصويب H2-a: المنفّذ يبدأ بـNEW — لا SUBMITTED).
`Room`: AVAILABLE / RESERVED / OCCUPIED / DIRTY / CLEANING / OUT_OF_ORDER (I12).
`AccessCode`: ACTIVE → (REVOKED | EXPIRED) · I11 يميته عند الخروج.
`ExtensionRequest / RoomChangeRequest`: PENDING → APPROVED/REJECTED → منفَّذة.

### 12.4 الهوية
أربع هويات: Booking Reference (تتبع فقط — لا دخول أبدًا) · Guest Code (H…) · Reception Code (R…) · Admin Code (A…) — توليد وتحقق وهَش SHA-256 وإبطال وتدقيق في `src/lib/codes.ts` + `src/app/api/auth/`. البادئات H/R/A حرف + 7 رموز.

### 12.5 سطح API المنفَّذ (تصويب الجرد H4-a: 67 ملفًا / 82 نقطة نهاية)
`/api/public/*` (6) · `/api/auth/*` (3: validate, logout, **renew بعد H3**) · `/api/guest/*` (16 نقطة) · `/api/reception/*` (23) · `/api/admin/*` (34) + `GET /api` (بقايا سقالة — مرشّح للحذف بقرار) — **الجرد الموثّق الكامل: `docs/CONTRACTS.md` v1.0** (أشكال الطلبات/الاستجابات/الأخطاء العربية الحرفية، مجموعة F1 معلَنة STABLE).

### 12.6 اصطلاحات التسمية (ملزمة كما في v1.0)
كيانات PascalCase مفرد · جداول snake_case جمع · حقول API camelCase · مسارات kebab-case · Enums UPPER_SNAKE · مراجع عامة `HTL-2026-000NNN` / `ST-…` / `REQ-…`.

### 12.7 الهوية البصرية
كحلي `#1A3C6E` + ذهبي `#D4A843` · خط Cairo · عربي RTL أولًا · ليلي/نهاري · (نسخة Flutter تحاكيها حرفيًا في F0).

---

## 13. الأكواد التجريبية (بيئة التطوير فقط — تُمسح قبل الإنتاج بموجب AD-10)

| الدور | الكود | الحالة في Seed |
|---|---|---|
| ضيف — خالد يوسف (غرفة 201) | `H834729X7` | إقامة جارية |
| ضيف — نورا سعيد (غرفة 103) | `H119922K4` | مغادرة اليوم |
| استقبال | `R492671M3` | فعال |
| إدارة | `A371849L9` | فعال |

---

## 14. سجل التغييرات

| الإصدار | التاريخ | التغيير |
|---|---|---|
| 1.0 | 2026-08-31 | وثيقة الرؤية التنفيذية الأصلية (مؤرشفة في `docs/archive/MASTER_PLAN_v1.md`) |
| **2.0** | **2026-09-01** | **إعادة البناء كخط سير حي:** جرد الواقع المنفَّذ · ADR v2 (Next.js هو backend الرسمي للـ v1 · SQLite للإنتاج الأحادي · Flutter نقل تصميم مثبت · قائمة التحصين المغلقة · تأجيل الاستخراج بمحفزات موثقة) · المسار الكامل H→F→W→P→O مع بوابات الثقة/المتجر/الإطلاق · حساب كل خطوة (قبول/اعتماد/جهد/خطر) · القوائم المجمدة · سجل المخاطر · مرجع الثوابت |
| **2.1** | **2026-09-02** | **إغلاق المرحلة H وبوابة الثقة (تنفيذ كامل متوازٍ):** H1 migrations (baseline 22 نموذجًا + مسار نظيف مجرّب) · H2 اختبارات المال (157 اختبارًا بأمر `bun run test` — وحدة/مصادقة/تكامل + الرحلة الذهبية بأرقام السنت) · H3 عقد الجوال (Bearer موحّد أصلًا + `renew` بلا أي migration — «الكوكيز» لم تلزم) · H4 CONTRACTS.md v1.0 (82 نقطة نهاية، مجموعة F1 معلنة STABLE) · تصويبات جرد: 22 نموذجًا، 67 ملفًا/82 نقطة، آلة ServiceRequest تبدأ NEW · **المرحلة التالية: F (Flutter)** |
| **2.2** | **2026-09-02** | **تنفيذ F0+F1+F2 (+F6 مبكرًا) بأمر المالك:** مشروع Flutter كامل في `mobile/` (38 ملف dart — نقل شاشة-بشاشة من مراجع الويب + عميل API بعقد §1.2.1 + Socket.IO + RTL/Cairo/كحلي-ذهبي) · **البناء والتشغيل والاختبار على GitHub Actions بقرار المالك** (mobile-ci: analyze+test+build smoke كلها خضراء · mobile-release: APK موقّع بمفتاح في أسرار GitHub → Releases · web-ci: 157 اختبارًا خضراء على GitHub) · **W0 مقفل بقرار المالك: بلا Twilio/Cloud API — wa.me + الأدمن يتحكم برقم الوجهة** (حقل قابل للتحرير في معالج الوصول) · المتبقي من F: F3 (Push) · F4/F5 (استقبال/إدارة) · F6-minAppVersion · F7 (Dogfooding) · F8 (المتاجر) |
| **2.3** | **2026-09-02** | **إغلاق F6 نهائيًا (Task 18 — أمر المالك: خطوة واحدة بلا توسيع نطاق):** minAppVersion كاملًا — migration `20260902000000_add_min_app_version` (عبر المسار الرسمي: resolve baseline + deploy) · PUB-07 نقطة عامة fail-open · حقل أدمن في A-03 + UI «أقل إصدار مسموح للتطبيق» + رسالة 400 حرفية · حارس Flutter عند الإطلاق (شاشة تحديث مطلوب — نسخ الرابط بالحافظة بلا حزم جديدة) + `APP_VERSION` يُدمج من pubspec في CI · CONTRACTS v1.1 (امتداد §9.1.2 موثَّق) · 165 اختبارًا (8 جديدة) + E2E متصفح · استعادة بيئة dev بعد إعادة تهيئة الحاضنة (seed الرسمي — نفس حالة H1 الموثقة) |
| **2.5** | **2026-09-04** | **إغلاق F5 كاملة (Task 21):** وضع الإدارة بأقسامه الإحدى عشر منقولًا إلى Flutter فوق AdminStore/A-01..A-34 بصفر تغيير خادم · وكلاء متوازيون (21-a أكواد/طاقم/تدقيق/ضيوف/تقارير · 21-b غرف/أنواع/معدلات · 21-c إعدادات/خدمات/حجوزات — تقارير agent-ctx/21-{a,b,c}) · 68 اختبارًا جديدًا (169 Mobile CI) + جولات CI أربع موثقة · إصلاح خلل حقيقي في AdminBarChart (فيض) + ApiClient.delete() · لا إصدار APK جديد (قرار المالك — الإصدار التالي يحمل وضع الإدارة تلقائيًا) |
| **2.4** | **2026-09-03** | **إغلاق F4 كاملة (Task 19 F4-a + Task 20 F4-b):** F4-b نقل المقيمين (R-04) + تفصيل الإقامة بخمسة تبويبات (R-05: ضيف/فاتورة R-12/R-13/طلبات/رسائل R-20/R-21/إجراءات R-15..R-18) + الطلبات بفلاترها وإدارة حالتها (R-08/R-09) + لوحة الغرف وانتقالاتها (R-10/R-11) + البحث العام (R-19) + القشرة 6 تبويبات (NavigationRail للواسع/تنقل سفلي للضيق — كالويب) + زر البحث في الهيدر + وصل كل نقاط F4-b المعلّقة في اللوحة · صفر تغيير خادم (القناة كاملة كانت متعاقدة ومنفَّذة ومستهلكة من الويب) · 7 شاشات/حوارات جديدة + 13+ اختبار شاشة · وكيلان متوازيان (تقاريرهما في agent-ctx/20-a·20-b) |

---

**خلاصة الوثيقة في سطرين:** ما بُني رأس مال مُثبَت لا يُعاد فتحه؛ وما ينقصه ليحمل عميل Mobile حقيقي أربعة أشياء بالضبط (migrations · اختبارات المال · عقد التوكن · وثيقة العقد المستقر) — وما بعدها: Flutter نقلًا لا استكشافًا، وواتساب مسار أعمال، والإنتاج انضباطًا لا مفاجأة، والمعمارية الأجمل تنتظر محفزًا واقعيًا لا مزاجًا.
